# 2026-09-11

## v1.0.7 封板（导入与更新模块）

按 spec 第 1 节「为 A/H 投研交易工作台新增『导入与更新』模块」（15 节、13 项禁止清单）封板。
**严格遵守"不为了导入功能重新设计数据库或投资规则"**。

### 核心实施
- `app/server.py` —— 升级 TARGET_SCHEMA_VERSION=1.0.7、Handler.server_version、argparse、启动 print 4 处版本号。
  新增 IMPORT_FORMAT_FULL / IMPORT_FORMAT_EXEC_ONLY / IMPORT_PREVIEW_CACHE 等常量；
  新增 `_import_identity_payload` / `_import_research_payload` / `_import_plan_payload` / `_import_execution_payload` / `_import_status_payload` 系列规范化与校验；
  新增 `preview_import_full` / `commit_import_full` / `preview_import_execution` / `commit_import_execution` 4 个业务函数；
  新增 4 个 API 路由（POST `/api/import/preview`、`/api/import/commit`、`/api/import/execution/preview`、`/api/import/execution/commit`）；
  `_run_in_transaction` 加 `@functools.wraps` 使事务函数支持 `__wrapped__` 内层调用；
  **既有业务函数 0 行改动**（create_security_tx / update_research_tx / update_plan_tx / add_execution_tx / ledger_add 全部保持）。
- `app/static/index.html` —— 左侧导航新增「导入与更新」入口。
- `app/static/app.js` —— 头注释升级到 v1.0.7；新增 `#/import`、`#/import/research`、`#/import/execution` 路由；
  新增 renderImportLanding / renderImportResearch / renderImportExecution 三组渲染函数；
  实现粘贴 → 解析 → 预览 → 确认 → 完成页的完整两段式交互。

### 测试
- `tests/test_v107.py` —— 新增 15 节 / 126 断言，全过。
  - §A 单只新证券完整导入 / §B 多只批量导入 / §C 已有证券正确识别
  - §D research 完全相同不生成重复 / §E research 变化生成新版本旧版保留
  - §F trade_plan 无变化不生成版本 / §G trade_plan 有变化生成新版本
  - §H execution append-only / §I 快速 execution 不修改 research/trade_plan
  - §J 快速 execution 找不到证券时拒绝 / §K 状态变化必须进入预览不得自动覆盖
  - §L 非法 JSON 不写库 / §M 导入失败不污染既有数据 / §N execution+ledger 原子性
  - §O trades 不得变化
- `tests/test_v102.py` —— schema_version 期望值改为动态读取 TARGET_SCHEMA_VERSION,适配 v1.0.7。
- 全部测试 279/279 PASS（v102=38 + v103=60 + v105=25 + v106=30 + v107=126）。

### 交付物
- `D:\个股工作台\个股工作台-v1.0.7-20260911.zip`（152,622 B, 17 entries, SHA `b794ebbd194acc684e2e9858858649ecb1f4168592644e77844211d0923ed001`）
- `output/20260910-audit/PACK_NOTES-v1.0.7.md`（9,713 B,封板说明）
- `output/20260910-audit/PACK_MANIFEST-v1.0.7.md`（3,573 B,14 条交付项 SHA256）
- `output/20260910-audit/BUILD_LOG-v1.0.7.md`（构建日志,ZIP 自身 SHA）
- `output/20260910-audit/stage3/A-H投研交易工作台交付审计文档-v1.0.7.docx`（49,403 B,SHA `631906a2c50d`,全新撰写,不复用 v1.0.6）
- `samples/json/example-full-import.json`（2,274 B,format=ah-workbench-import）
- `samples/json/example-execution-update.json`（504 B,format=ah-workbench-execution）
- `output/20260910-audit/screenshots/screenshot-1-full-import-done.svg`（导入完成页视觉示意）
- `output/20260910-audit/screenshots/screenshot-2-execution-update-preview.svg`（执行预览页视觉示意）

### 数据库状态
- `data/workbench.db` —— schema_version 升级 1.0.6 → 1.0.7
- FK violations = 0,integrity_check = ok
- DB size 81,920 B,SHA `0d847b5d924af014`（与 v1.0.6 一致,仅 schema_version 变更）

### 重要边界
- 导入路径不调用 `add_trade_tx`,所以 trades 行数始终为 0（§O 验证）。
- 导入路径通过 `__wrapped__` 进入既有事务函数内部,确保**整批导入统辖在一个 SQLite 事务**;任一只标的失败 → 整批 ROLLBACK（§M 通过 `_LEDGER_FAIL_INJECT` 验证）。
- 状态变化必须用户在预览中显式勾选（`status_change_confirmed=true`）,否则 commit 阶段被拒绝（§K）。
- 已存在的 securities 拒绝走 `create_security_tx`(因为该函数内部会有重复检查);新建路径走 `create_security_tx` 一致方式,但通过 **同一事务** 串接 research v1 + plan v1 创建。
- agent-browser 在受限环境无法启动 Chromium 截图,改用 SVG 绘制视觉示意;**内容基于真实 HTML/CSS/JS 渲染**。

### 用户下一步可能的方向
1. 审计方认可 → 进入"把已深穿过的候选标的正式导入工作台"（用 `example-full-import.json` 模板填充实际标的）。
2. 审计方提出新一轮 spec（v1.0.8）→ 按相同封板流程执行。
3. 临时产物清理（A/B/C/D 选项,等用户指示）。

---

## v1.0.8 封板（导入完整性封板修复）

spec：9 节，只修 v1.0.7 独立代码审计发现的导入完整性与交付一致性问题，**不得新增业务模块**。

### 九条修复落地（`app/server.py`，既有业务函数 0 行改动）

| 审计问题 | 实现 |
|---|---|
| 1 base64 token 不能证明 preview 发生过 | `secrets.token_urlsafe(32)` 服务端随机 token + `_IMPORT_PREVIEW_CACHE` + `IMPORT_PREVIEW_TTL_SECONDS=300` + commit 后一次性失效。`_import_cache_put/take/invalidate` |
| 2 状态确认来自导入 JSON | 导入 JSON 的 `status_change_confirmed` 一律忽略；preview 初次 `change_confirmed` 恒 False；确认只来自 commit 请求的 `confirmed_status_changes`（支持 `[0,2]` 下标或 `[{exchange,code}]` 身份） |
| 3 commit 前未校验 preview snapshot | `_import_snapshot_for` / `_import_snapshot_drift` / `_import_require_no_drift`，严格只判 4 类漂移（status / research version / plan version / 原不存在证券已出现）+ 对称的"已存在证券被删除" |
| 4 同批可重复证券 | `_parse_import_full` preview 前 `(exchange, code)` 唯一性检查，重复整体拒绝 |
| 5 研究数组结构未严格校验 | `_import_validate_kv_list`：`core_validations[]/{content,status∈跟踪中\|已验证\|已恶化}`、`wall_conditions[]/{content,triggered 严格布尔}`。**不猜测、不自动转换**（`"false"` 与 `0/1` 均拒） |
| 6 name 不一致仅暗藏 | preview 返回 `name_mismatch` + `name_mismatch_notice` 并进顶层 `warnings`；前端渲染"名称不一致"横幅 + "需人工核对"汇总条。本轮不自动改 name |
| 7 危险真实证券示例 | 删除「载入示例」按钮（`app.js` 注释标注原因）；样例改为明显虚构 HK.90001 / SH.900001，带 `_notice`"请勿写入正式库" |
| 8 缺回归测试 | `tests/test_v108.py` 129 断言 + `smoke_v108_http.py` 14 断言（真实 HTTP 链路） |
| 9 交付包不一致 | 见下 |

关键设计：漂移检测**不消费 token**（用户可重新预览）；写入仍为单一 SQLite 事务，任一只失败整批 ROLLBACK。

### 交付包（v1.0.8）

- `个股工作台-v1.0.8-20260911.zip` — 178,621 B / 23 entries /
  SHA256 `afe9312964314744ceefff15ff5e6dbedd6d6b4772b8b68f9088fdbebb7591bd`
- 测试实跑：离线 407 PASS（38+60+25+28+127+129）+ 集成 14 + HTTP 冒烟 14，FAIL 0
- Manifest 23 条 ↔ ZIP 23 条一一对应，无幽灵条目；逐文件尺寸/SHA 全对
- README 默认地址 `http://127.0.0.1:8765`；`启动工作台.bat` 改 PATH 查 python（去绝对路径）
- DB：schema_version 1.0.8，integrity_check=ok，foreign_key_check 0 违例，securities=0 / trades=0

### 本轮关键教训：审计文档必须实跑核对

初版 v1.0.8 ZIP（10:10 构建，SHA `6f37fe12…`）已封板，但我在交叉核对时发现
审计文档 §8.2 有**不实表述**：

- 文档写"test_v107.py 原有 **127** 断言…其余 **124** 条业务语义断言未做任何修改"
- 实跑 v1.0.7 原始 ZIP 内 `tests/test_v107.py` → `TOTAL = **126**`
- 逐标签 diff 实际有 **5** 条被改写：`§K#7`、`§K#8`（**业务语义**，因状态确认契约变更而必须重写，
  且 §K#7 拆为 §K#7+§K#7b 使断言更严）、`§P#1`、`§P#2`、`§P#6`（版本契约）
- 正确结论：126 → 127，改写 5 条，**121 条**逐字未动

已修正 `build_docx_v108.py` + `PACK_NOTES-v1.0.8.md`，重建 docx（44,532 B /
`9ab1681cee454f97…`）→ Manifest → ZIP（新 SHA `afe93129…`）。
作废版留档 `.tmp_v108x/verify/superseded-v1.0.8-old.zip`；重建记录写入 `BUILD_LOG-v1.0.8.md`。
验证探针与打包脚本留档 `.tmp_v108x/verify/`（`probe.py` / `probe_a1.py` / `build_pack_v108.py`）。

> 教训：**"其余 N 条未做任何修改"这类话必须逐标签 diff 得出，不能靠断言总数反推。**

### 工作区侧的遗留观察（非本轮任务）

- 根目录 `PACK_MANIFEST.md` / `PACK_NOTES.md`（v1.0.5 时期遗留）与各版本 ZIP 并存，
  版本归属易混；`archive/`、`audit_pack_src/`、`pack_v106_src/`、`.tmp_v108x/` 均为历史暂存目录。
  如需清理请单独指示。
- `git` 仓库未初始化（无版本控制），版本追溯全靠 ZIP 与 memory。

### 交付前对抗性验证：发现两个真实缺陷（R-027 / R-028）

不满足于"跑测试文件"，另写了并发探针 `.tmp_v108x/verify/probe_race.py` 打真实 HTTP，
**两个缺陷均 3/3 稳定复现**：

- **R-027 同 token 并发 commit 可重复追加 execution**
  同一 preview token 并发 8 次 commit → **3 次返回 200，`execution_reviews` 追加 3 条（应为 1 条）**。
  根因：`_import_cache_take()` 只读 cache 不消费，token 在 commit 成功后才 `_import_cache_invalidate()`，
  存在 TOCTOU 窗口；而 spec 要求的 **4 类漂移检测不覆盖"新增 execution"这类 append-only 写入**
  （status / research version / trade_plan version 都不变）。
  对照：新建证券 / 改 status / 改 research 或 plan 的场景，漂移检测能拦住（6 线程并发仅 1 次成功）。
- **R-028 并发返回 HTTP 500**
  8 并发中 4~5 次返回 `500 服务器错误: database is locked` 而非 400。
  根因：`_mut()` 只映射 `sqlite3.IntegrityError` → 400，未捕获 `sqlite3.OperationalError`。

**处置：只登记、不改代码**（本轮 spec 限定"只修 v1.0.7 的 9 项、不得扩展任务"）。
已写入 docx §11 风险表 + §12 已知缺陷复现项 + PACK_NOTES §十/§八。建议 v1.0.9 修：
cache 加 per-token `in-flight` 标记（take 时原子置位、失败释放、成功失效）；`OperationalError` 映射 400。

### v1.0.8 最终交付（3 次作废 + 1 次有效，代码零改动）

| 项 | 值 |
|---|---|
| ZIP | `个股工作台-v1.0.8-20260911.zip`，180,933 B，23 entries |
| SHA256 | `c852eb3b900df1d346a06480c3c863b40a8bba8e5590698abfff6412e2d0b06d` |
| docx | 45,764 B，`e51a829e…`；PACK_NOTES 11,267 B，`726c93ee…`；Manifest 3,708 B，`752b45e3…` |
| 测试 | 离线 407 + 集成 14 + 冒烟 14，FAIL 0（解压全新目录复跑通过） |
| 校验器 | `ah-workbench-release-verify` 技能：17/17 PASS |
| 代码 | `app/server.py` = `143d1d57…`、`app/static/app.js` = `8748054b…`，**全程未变** |

**版本谱系**（每次作废都因"文档与事实不符"，这才是本版要根治的病）：

1. `6f37fe12…` 177,966 B —— §8.2 称"原有 127 断言 / 其余 124 条未改"，实为 **126**、且 **5** 条被改写
2. `afe93129…` 178,621 B —— 修好 §8.2，但尚未发现 R-027 / R-028
3. `553e23f2…` 180,448 B —— 补了 R-027/028，却把并发**次数写死**为"3 次 / 3 条 / 4~5 次"，
   而实测是 **2~3 次**波动 → 仍是"不可复现的声称"
4. `c852eb3b…` 180,933 B —— **当前有效**：次数改为区间 + **不变式**
   （成功次数恒 > 1、append-only 表恒被多写、5xx 恒 > 0）

留档：`.tmp_v108x/verify/superseded-r1-docx-claim-false.zip`、`superseded-r2-fixed-counts.zip`；
探针 `probe.py` / `probe_a1.py` / `probe_race.py` / `build_pack_v108.py`。

> 两条方法论教训，已写入技能：
> 1. 并发缺陷的"成功次数"随线程调度波动，**写死具体次数同样属于不可复现的声称**；
>    应给不变式 + 波动区间。
> 2. 凡是"一次性凭据 / token / append-only 写入"的设计，**必须跑并发探针**；
>    顺序路径全绿完全不能证明没有缺陷（本版 407 断言全过 + 14 类审计项全过，仍漏掉两个真实缺陷）。

> 建立的可复用资产：技能 `ah-workbench-release-verify`
> （`scripts/verify_pack.py` 一条命令完成 Manifest↔ZIP + ZIP↔工作区 + 解压重跑 + 端口探测；
> `scripts/concurrency_probe.py` 并发一致性探针）。

---

## v1.0.9「最终并发与预览一致性修复」封板（11:20–11:35）

单轮完成，**只改 2 个源文件**，无 Schema 变化、无业务功能变化。

### 四条修复

1. **R-027 修复（并发重复提交）**：删除 `_import_cache_take()`（只读不消费），
   改为 `_import_cache_claim()` —— 在 `_IMPORT_PREVIEW_LOCK` 内一次性完成
   「存在 / TTL / kind / **in_flight**」判定并原子置位 `in_flight`。
   新增 `_import_cache_release()`（失败释放，覆盖 claim 之后所有异常，
   含参数不合法与 `get_db()` 自身失败）；`_import_token_key()` 统一 strip，
   使「token 前后加空格」也无法绕过一次性失效。full 与 exec-only 共用同一机制。
2. **前端最小防重**：`Import.committing` 状态；点击后按钮立即 disabled + 「正在写入…」，
   请求返回前不发第二次，失败恢复。仅为 UI 防误操作，后端原子防重独立存在。
3. **execution_latest_id 纳入漂移检测**：snapshot 每条打 `check_execution`；
   仅 exec-only 与「完整导入中带 execution 块」的证券比对；
   **不带 execution 块时不被无关 execution 更新阻断**。
   `_import_snapshot_drift()` 返回值由 `list[str]` 改为 `list[(kind, str)]`，
   `_import_require_no_drift()` 按 kind 选提示语 ——
   新增 execution 专属文案，**原「工作台数据自预览后已发生变化…」逐字不变**（旧测试有硬断言）。
4. **R-028 修复（409 语义）**：新增 `DbConflictError` 与 `_is_db_busy_error()`
   （优先 `sqlite_errorcode & 0xFF in (5,6)`，文本匹配兜底）；
   `_mut()` 中 `OperationalError` → busy 走 409「数据库正在处理另一项写入，请稍后重试。」，
   其余仍 500 —— **不得把 no such table / I/O 伪装成 4xx**。

### 验证结果（全部真实运行）

| 项 | 结果 |
|---|---|
| 离线 | **490 断言**（38+60+25+28+127+129+**83**），FAIL 0 |
| 新增 `tests/test_v109.py` | **83 断言**（§A–§I：并发 / 漂移 / 防重 / 409 / 静态契约） |
| 新增 `smoke_v109_http.py` | **38 PASS**（全部验收点的真实 HTTP 端到端） |
| 回归 | `smoke_v108_http.py` 14 PASS、`test_integration_quote.py` 14 PASS |
| **稳定性** | `test_v109.py` 连跑 **5 次**、`smoke_v109_http.py` 连跑 **3 次**：每次 §A 并发成功次数**严格 = 1**、§B ≤ 1 |
| R-027/028 原探针 | `concurrency_probe.py --n 8 --trials 3` → `{400:7, 200:1}` ×3，0/3 需人工判定 |

### 关键教训（v1.0.8 的隐患已消除）

- v1.0.8 并发实测是 **2~3 次**波动 → 当时只能写区间；v1.0.9 修复后变为**恒定 1 次**，
  终于可以写"严格 = 1"。**能写死等号，本身就是缺陷被修掉的证据。**
- `_import_cache_take` 那种「先读后写、成功才失效」的模式是 TOCTOU 经典形态；
  正确做法是把**校验与占位合并进同一个临界区**。

### 交付

| 项 | 值 |
|---|---|
| ZIP | `个股工作台-v1.0.9-20260911.zip`，201,357 B，25 entries |
| ZIP SHA256 | `7cc93181516a40fc34c8fcf99cb8bccc5c230f25029bdbd11a72808bb3e60aa3` |
| docx | 47,227 B，`7f26486e…`；PACK_NOTES 11,248 B，`1aa650c5…`；Manifest 4,288 B，`2c6946f8…` |
| 代码 | `app/server.py` = `be8dd23f…`（148,060 B）、`app.js` = `9855a889…`（84,469 B） |
| 校验器 | 技能 `verify_pack.py`：**18/18 PASS**，端口探测 12/12 PASS |
| 版本谱系 | 初版 `7287ff33…`（201,363 B）→ 因 Manifest 一句**否定式免责声明**含 `build_docx_v10x.py` 字样、易被粗粒度扫描误报而重打 → 现行 `7cc93181…`（未留档，仅该句不同） |

### 工作区状态变更

- `data/workbench.db` 已由 1.0.8 迁移至 **1.0.9**（空库，`integrity_check=ok`，FK 违例 0，
  表清单未变）；迁移前留档 `.tmp_v108x/verify/wsdb-before-109.db`。
- 本轮新增工作区脚本：`output/20260910-audit/build_docx_v109.py`、
  `.tmp_v108x/verify/build_pack_v109.py`（均**不进交付包**）。
- **修正了技能自身的缺陷**：`verify_pack.py` 的「Manifest 未列构建脚本」原为整份文本正则匹配，
  会把否定句误报为违规；已收敛为只检查清单条目行（`| 序号 | \`路径\` | … |`）。

---

## 启动器修复：`启动工作台.bat` 无法启动（11:46 起）

### 根因（已实测确认，非推测）

旧 bat（388 B，`caf74fb14c04cbef…`）用 `where python` 判定解释器是否可用，逻辑是：

```bat
where python >nul 2>nul
if errorlevel 1 ( echo [ERROR] Python not found. ... )
```

**该判定在本机永远为"通过"，但通过的是假解释器。** 实测：

| 探测项 | 实测结果 |
|---|---|
| 用户持久 PATH（`HKCU\Environment`） | **只有** `%USERPROFILE%\AppData\Local\Microsoft\WindowsApps` |
| 系统持久 PATH 中与 python 相关项 | 只有 `C:\Windows\system32\config\systemprofile\...\WindowsApps` |
| 该目录下的 `python.exe` | **Microsoft Store 执行别名存根**，不是解释器 |
| 真正可用的解释器 | `%USERPROFILE%\.workbuddy\binaries\python\versions\3.13.12\python.exe`（**不在持久 PATH 中**） |

因此双击 bat 时：`where python` 命中存根 → 「Python not found」分支从不触发 →
`python app\server.py` 实际拉起 Store 存根 → 窗口一闪而过 / 弹应用商店。

**诊断陷阱**：在 agent 的 Git Bash 里 `where python` 与 `python --version` **都正常**
（受管运行时被注入到进程 PATH 且排在 WindowsApps 之前），所以只在本机 shell 里验证会得出
"bat 没问题"的错误结论。必须读注册表的**持久** PATH 才能复现用户侧现象。

### 修复设计（三条要点）

1. **`%USERPROFILE%` 代替硬编码绝对路径**。受管解释器在 `C:\Users\宜春法院\...` 下，用户名含中文。
   直接写字面路径会让 bat 含非 ASCII 字节 → cmd.exe 按控制台代码页（936/65001）逐字节解释时错位、
   污染传给 shell 的路径。历史包 `audit_pack_src/启动工作台.bat` 正是踩了这个坑（注释被写成拼音，
   路径段出现 `M-RM-KM-4M-:M-7M-(M-TM-:` 这类乱码字节）。
   改用 `%USERPROFILE%\.workbuddy\...` 后**源码 100% ASCII，运行时仍解出正确绝对路径**。
2. **候选链 + 真执行探测**：① pinned `3.13.12` → ② `for /d` 遍历 `versions\*` 任意受管版本 →
   ③ `py` → ④ PATH 上的 `python`。每个候选交由 `:try` 子程序用
   `-c "import sys;raise SystemExit(0 if sys.version_info>=(3,8) else 1)"` 真跑一次，
   并**显式拒绝**路径中含 `WindowsApps` 的候选（`if not "%T:WindowsApps=%"=="%T%" goto :eof`）。
3. **前后置守卫**：启动前检查 `app\server.py` 是否存在；找不到解释器时给出明确指引
   （并点明 WindowsApps 里那个只是 Store 占位符）。

### 验证（全部实测）

- 生成物：**3,970 B / 纯 ASCII / 105 个 CRLF / 0 个裸 LF**，SHA-256 `65414590d0804b255830ce6b489e384db7501659ccb50b9ebf8c0dc64f70fb93`。
- **端到端**：实际执行 bat → 输出 `Python : "C:\Users\宜春法院\.workbuddy\binaries\python\versions\3.13.12\python.exe"`
  （中文路径展开正确）→ 监听到 `127.0.0.1:8765` → `HTTP 200`，`Server: Workbench/1.0.9 Python/3.13.14`。
- **`:try` 隔离测试**：从**已安装的 bat 里抽取**该子程序（保证被测代码 == 交付代码），装进测试壳跑三类候选，
  3/3 PASS —— 受管解释器被接受；不存在路径被拒绝；`...\WindowsApps\python.exe` 被路径守卫拒绝。
- 生成器留在 `.tmp_v108x/verify/make_launcher.py`（临时工具，不进交付包）。

### 遗留决策（未自行推进）

工作区 bat 已修好；但 **v1.0.9 交付包里的 bat 仍是旧的**（388 B / `caf74fb1…`，Manifest 第 06 条）。
同版本号重打会产生"一个版本号两个 SHA"，违反本项目交付约定。三个选项待用户拍板：
① 升 v1.0.10 重新封板（推荐，但需同步 4 处版本 + 测试版本契约 + 重算 Manifest/审计文档）；
② 原地重打 v1.0.9（快，但破坏交付可追溯性）；③ 暂不动交付包，只保本机可用。
另：根目录 `个股工作台-v1.0.9-20260911.zip` 与 `README.txt` 已被用户取走，当前**不在工作区**。

### 环境坑（新增）

- **PowerShell 工具在本会话下 stdout 完全捕获不到**（`Write-Output "hello"` 也无输出，exit 0）。
  需要读注册表/持久环境变量时，改用 `python -c "import winreg; ..."` 从 Bash 调。
- **`cmd.exe /c ...` 被安全策略拦截**（"Invoking cmd.exe from Bash bypasses all command validation"）。
  要执行 .bat 请用 `python -c "subprocess.run([abs_bat_path])"`，且**必须传绝对路径**——
  传 `.tmp_v108x/verify/x.bat` 这种相对正斜杠路径会被 CreateProcess 拒绝，
  且失败时 stdout 为空、容易被误判成"断言通过"（本次就踩到：候选1 假 FAIL、候选2/3 假 PASS）。
- `open(path, 'r', encoding='ascii')` 读 CRLF 文件时若不加 `newline=''`，universal newlines 会把
  `\r\n` 折成 `\n`，导致 `bat.index(':try\r\n')` 抛 `ValueError: substring not found`。

---

## 12:00–12:25 —— v1.0.10「启动脚本交付修复」封板（用户批准走"升版重新封板"路线）

### 起因与决策

用户指令「更新 bat 文件，可以正常启动工作台」。现场勘察发现两件事：

1. **工作区那个 bat（3,970 B）已经是修好的**，实测能拉起服务 —— 所以用户说的必然是
   **交付包内那个旧 bat（388 B / `caf74fb1…`）**。
2. 工作区处于一处**未完成的版本升级**：`server.py` 的 `TARGET_SCHEMA_VERSION` /
   `server_version` / `argparse` 已是 `1.0.10`，但**启动 print 文案仍是 `1.0.9`**，
   `app.js` 版本头与 3 个测试套件的版本契约也都还停在 1.0.9 —— **套件是红的**。

因交付包版本决策此前明确挂在"待用户拍板"，故先问了一次，用户选择
**① 完成 v1.0.10 重新封板**。

### 交付物

| 项 | 值 |
|---|---|
| ZIP | `个股工作台-v1.0.10-20260911.zip` |
| 字节 / SHA-256 | 208,012 / `c55fdc6abfa6d45f4477177f0f3943ff58d85cbe2fb1ea33a867925afd3b3e46` |
| 条目数 | 26（25 + Manifest 自身） |
| 离线断言 | **504**（38+60+25+28+127+129+83+14），FAIL 0 |
| 校验器 | `--rerun` 26/26 PASS；`--port-probe` 全 PASS |
| 端到端 | 11/11 PASS |

### 本轮实质改动

- `启动工作台.bat` 整体替换（388 B → 3,970 B）：4 级候选链 + `:try` 真执行探测
  （`sys.version_info>=(3,8)`）+ 显式拒绝 `WindowsApps` 路径 + 前后置守卫。
- `server.py` ×4 处版本号 + 头部说明注释；`app.js` 版本头 + 头部说明。
- 新增 `tests/test_v110.py`（14 断言）：§A 版本同步契约（含 §A#5 四值唯一性）、
  §B 启动脚本契约（8 条）。
- 既有测试仅 11 条版本契约前移（test_v107 ×2、test_v108 ×3、test_v109 ×6），
  三个套件断言总数不变（127/129/83）；离线 490 → 504 净增 14，来源唯一。

### 三个最值得记住的点

1. **核心教训三（方法论证）**：v1.0.9 服务端全绿，缺陷却出在"我们测它的环境"里。
   agent 进程 PATH 被注入过受管运行时，`where python` 正常；用户**持久 PATH** 里
   只有 Microsoft Store 存根。**凡"能不能跑起来"的断言，必须先在干净环境复现用户处境。**
2. **交付库 ≠ 工作区库**：v1.0.9 打包时工作区库恰是空的，两份天然一致；
   本次工作区库已有 7 只真实标的。按"空库交付"约定，打包脚本用 `SRC_OVERRIDE`
   指向临时生成的空库模板，**不动工作区库**；校验器 `[2]` 排除该文件，
   改由 `[3]` 按 `integrity / FK / schema_version / 必须为空` 四项校验 —— 强度不降反升。
3. **契约测试必须负向验证**：把旧 bat 放回原位跑 `test_v110.py` 得 3 FAIL
   （§B#4/#B#5/#B#6），恢复后 SHA 回到 `65414590…`。这证明测试有效，
   也支撑了审计文档那句"该测试若在 v1.0.9 封板时存在，本缺陷不会漏出"。

### 自身踩到并修掉的两个坑

- **模块 docstring 里的 Windows 路径**：写入 `%LOCALAPPDATA%\Microsoft\WindowsApps`
  与 `versions\*` 触发 `SyntaxWarning: invalid escape sequence '\M'` / `'\*'`。
  改为 `\\` 与改写措辞；用 `py_compile(doraise=True)` + `-W error::SyntaxWarning` 复验通过。
- **端到端探针的收尾**：`Popen.kill()` 只杀 `cmd.exe`，子进程 `python.exe` 存活并继续
  监听 8765，导致实验 B 误判"旧 bat 也留下了监听端口"（假 FAIL）。
  改为按端口属主 `taskkill /F /T`，并新增 A#4 断言"端口已彻底释放"。

### 版本历史

v1.0.7（导入交互层）→ v1.0.8（导入完整性，407）→ v1.0.9（并发与预览一致性，490）
→ **v1.0.10（启动脚本交付修复，504）**

### 收尾

- 根目录 `README.txt` 按约定打包后移除（副本在 ZIP 内）；探针残留 `_ascii_test.bat`、
  临时解压目录已清理；8765 端口与 python 进程均已确认清空。
- 技能已更新：`ah-workbench-release-verify` → v1.1.0（新增 Step 3b 契约测试+负向验证、
  Step 3c 交付库/工作区库分离、Step 5b 三段式验证、核心教训三、两条环境陷阱）。

---

## 12:25–12:35 项目纳入 Git 版本管理并推送 GitHub

用户要求「把项目推送到我的仓库上」，给出 `git@github.com:healyirobert99-dotcom/gzt.git`。

### 现场摸底结果

- 本机 **没有任何 GitHub 凭据**：无 `~/.ssh` 目录、无 gh CLI、无 credential helper、无 token；
  `git config --global user.name/user.email` 均未设。git 2.55.0.windows.3 可用。
- 工作区**尚未初始化 git**（一直在待办清单里）。
- 目标仓库 `gzt` **已存在、为空、且是公开仓库**（匿名 `git ls-remote` exit=0 无输出；
  网页返回 200；不存在的仓库会是 404）。
- 全工作区 177 个文件 / 5.5 MB，最大文件 203 KB（交付 ZIP）→ 无体积障碍。

### 用户拍板的四项决策

1. **数据范围** —— 选择「包含数据库」（真实 7 只标的 + `data/backup/` 12 份备份都推）
2. **历史归档** —— 选择「一并入库」（`archive/` `audit_pack_src/` `pack_v106_src/` `.tmp_v108x/`）
3. **提交身份** —— GitHub 用户名 + `@users.noreply.github.com` 匿名邮箱
4. **认证方式** —— SSH 公钥（而非 PAT）

> 因仓库公开，已当面告知：真实标的、研究结论与交易计划将对互联网可见。

### 完成的工作

- `git init` + 默认分支设为 `main`；`--local` 配置身份、`core.quotepath=false`、
  `core.autocrlf=false`（保证入库字节与工作区逐字节一致）。
- 写 `.gitignore`：只排 `__pycache__/`、`*.py[cod]`、根目录 0 字节 `workbench.db`、
  系统与编辑器垃圾。实测被忽略的确实只有这两个。
- 首次提交 `eb91952`：**176 文件 / 51,282 行插入**，工作区提交后干净。
- 生成项目专用密钥 `~/.ssh/id_ed25519_gzt`（ed25519，无口令），写 `~/.ssh/config`
  （`IdentityFile` + `IdentitiesOnly yes`），`chmod 600`。
- `git remote add origin git@github.com:healyirobert99-dotcom/gzt.git`。

### 踩到的坑（已写入 MEMORY.md）

**MSYS2 版 ssh 在中文用户名 HOME 下彻底不可用。** `which ssh` → `/usr/bin/ssh`
（OpenSSH 10.3p1，MSYS2 编译），它把 `HOME=/c/Users/宜春法院` 按本地 ANSI(GBK) 解读，
去找 `/c/Users/\322\313\264\272\267\250\324\272/.ssh/known_hosts` —— 于是
**`known_hosts` 与 `~/.ssh/config` 双双读不到**，报 `Host key verification failed`。
表象是"主机密钥问题"，实际是路径编码问题，极易误诊成密钥没配好。

自检手法（很值得记）：`ssh -G github.com | grep '^user'`
→ MSYS2 版输出 `user 宜春法院`（config 未生效），Windows 原生版输出 `user git`（正常）。

**解法**：`git config core.sshCommand "C:/Windows/System32/OpenSSH/ssh.exe"`，
改用 Windows 原生 OpenSSH 9.5p2（用 Windows API 读路径，中文正常）。
切换后 `-G` 正确解析出 `IdentityFile ~/.ssh/id_ed25519_gzt`、
`UserKnownHostsFile C:\Users\宜春法院/.ssh/known_hosts`，报错退化为预期的
`Permission denied (publickey)` —— 即只差用户把公钥加到 GitHub。

### 待用户操作

公钥 `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIP6EMgv62AH2iPPSwwGbinEwKOUhdQhgQl3jcJhNzuRw
healyirobert99-dotcom@gzt` 需添加到 GitHub → Settings → SSH and GPG keys。
添加完成后执行 `git push -u origin main` 即可。