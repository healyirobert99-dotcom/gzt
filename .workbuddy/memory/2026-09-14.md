# 2026-09-14

## 09:06 云端仓库覆盖本地（远程为权威源的首次同步）

用户指令：「把云端仓库覆盖本地版本」。

### 背景

9/11 完成首次推送（本地 4511cfc ↔ 远程一致）之后，用户在 **GitHub 网页端**继续维护了仓库
（提交消息为英文风格：`feat: refine A/H workbench lifecycle UI` 等，含 "upload" 字样，
判定为网页上传操作），远程前进到 `70cbed3`。9/14 用户要求以远程覆盖本地。

### 执行

1. 摸底：本地 4511cfc 工作区**完全干净**（0 改动、0 未跟踪）→ 覆盖无任何本地损失。
2. `git fetch origin` + `git log FETCH_HEAD` 看清远程新增 6 个提交：
   - `e342fbc` feat: refine A/H workbench lifecycle UI
   - `01f9192` feat: update workbench frontend assets
   - `53432fa` data: update workbench database
   - `72fbbb3` audit: add UI review package
   - `f5384c0` audit: add frontend review files
   - `70cbed3` chore: remove flattened upload duplicate
3. **祖先关系确认**：merge-base(4511cfc, 70cbed3) = 4511cfc → 远程是本地的直接后代，
   **纯 fast-forward，本地提交零丢失**。
   （探针教训：shell 里拿短 hash 与 `git merge-base` 输出的长 hash 做字符串相等比较，
   必然走错分支——脚本误报"远程改写了历史"，实际没有。比较要么截断要么用完整 hash。）
4. `git reset --hard 70cbed3`（**用显式完整 hash**，不依赖 `origin/main` 引用——
   本环境的文件拦截会清除 `.git/refs/remotes/` 下新建的引用，fetch 建立的
   `refs/remotes/origin/main` 在同一命令内就已消失，`git status` 恒显示 `[gone]`）。

### 覆盖后验证

- 本地 HEAD == 远程 main == `70cbed3`；树对象 `49ec82d4`；跟踪文件 176 → **190**。
- 新增内容：`app/static/logo.svg`、根目录平铺 `index.html / logo.svg / style.css /
  workbench.db`、`output/20260911-ui-audit/`（UI 审计包，含 frontend 副本与 SHA256SUMS）。
- `app/static/index.html`（±32 行）、`app/static/style.css`（+390 行）有修改；
  `app/static/app.js` 未变。
- **`data/workbench.db` 90112 → 147456 字节**（用户在云端演进的新库）：
  - integrity ok、FK 违例 0、schema_version 1.0.10
  - 证券 7 → **16 只**（新增：紫金矿业/金力永磁/盛弘股份/华明装备/万华化学/
    永和股份/毛戈平/锡业股份/柏楚电子），research 17、trade_plans 21、
    execution_reviews 29、decision_ledger 57、trades 0
- `git status` 覆盖后 0 改动。

### 观察与提醒（已告知用户）

- **根目录的平铺文件是网页上传产生的重复**：`index.html / logo.svg / style.css /
  workbench.db` 与 `app/static/`、`data/` 下的正式文件重复。远程最后一笔提交名为
  "remove flattened upload duplicate"，但这些平铺文件仍在（可能是删了一部分后重传）。
  清理需用户指示：本地删除 → 提交 → 推送。
- `.gitignore` 的 `/workbench.db` 规则与被跟踪的根目录 `workbench.db` 共存
  （ignore 只对未跟踪文件生效），无实际影响。
- 环境会清除 `.git/refs/remotes/*`：以后同步一律用
  `git fetch` → `git ls-remote` 拿显式 hash → `git reset --hard <hash>`，
  不要指望 `origin/main` 引用存在。

### 未做（超出本轮指令范围）

- 未启动服务做运行冒烟（server 启动会触发 init_db 写库，弄脏刚同步的工作区）。
- 未清理根目录平铺重复文件（需用户指示）。

### 追加：两笔善后提交（已推送）

1. **`6c71442`** 记忆同步提交里意外混入 `data/workbench.db-shm`（32KB）与
   `data/workbench.db-wal`（0B）——是本轮**只读检查数据库**（`mode=ro` 连接 WAL 库）
   时 SQLite 生成的运行时伴生文件，被 `git add -A` 收进。
   - 数据无损失（-wal 0 字节 = 全部已 checkpoint；主库 integrity ok、16 只证券完好）
   - **教训：对含 SQLite 库的项目，`git add -A` 前必须检查有没有新生成的
     `-shm` / `-wal` / `-journal` 文件**；只读连接也会产生它们
2. **`1435a06`** 善后：`git rm --cached` 移出跟踪（工作区文件保留不删），
   `.gitignore` 补 `*.db-shm` / `*.db-wal` / `*.db-journal` 规则，已推送。

最终状态：本地 = 远程 = `1435a06`，工作区 0 改动。

### 本轮还确认的一个环境陷阱

连续多次 Edit 同一文件时，**部分编辑可能被环境静默回滚**（工具返回成功但内容
未持久化）：9/11 对 MEMORY.md 待办段的更新（移除「仓库尚未初始化 git」等）
在 `4511cfc` 提交时就不在文件里，直到本轮覆盖后才暴露。
**对策（已写入 MEMORY.md）：关键编辑后必须 grep 验证；commit 前 git diff 复核。**

## 清理根目录平铺上传残留（用户指示：以云端版本为准）

- **判定依据（git 历史逐层核实，非猜测）**：云端 6 提交时序为
  `e342fbc`（平铺上传旧 UI + 旧 DB）→ `01f9192`（正式位置 app/static/ 更新前端）
  → `53432fa`（data/ 更新数据库）→ 审计包 ×2 → `70cbed3`（只删了根目录 app.js）。
  即 `app/static/` 与 `data/` 是云端维护的**更新、更全**的正式版本；
  根目录 4 个平铺文件是更早的上传残留（index.html 1311 B vs 2051 B，
  style.css 29 KB vs 46 KB；logo.svg / workbench.db 与正式文件逐字节一致）。
- **执行**：`git rm index.html logo.svg style.css workbench.db` → 提交 → 推送。
  删除后核验：正式文件完好（data/workbench.db integrity=ok、16 只标的在位）。
- **结果**：本地 = 远程 = `6615916`，跟踪文件 190 → 187，工作区 0 改动。
- **注意**：`.gitignore` 的 `/workbench.db` 规则此前因文件已被跟踪而失效，
  清理后才真正生效。

## 桌面启动器（用户要求：桌面创建 bat 启动文件）

**设计**：薄封装 + 委托，不复制解释器探测逻辑——桌面 bat 只做
「守卫检查 → `call "%WB%\<项目启动器>"`」；WB 硬编码 `D:\个股工作台`。
理由：项目内那份 4 级候选链已通过 `test_v110.py` 契约测试，复制一份必然漂移。

**编码决策（关键）**：项目内启动器"必须 ASCII"，而桌面这份**必须含中文路径**，
方向相反。采用 **UTF-8 无 BOM + CRLF + 第 2 行 `chcp 65001 >nul`**，
且保证**首个非 ASCII 行（第 13 行）之前全是 ASCII**——cmd 先按默认代码页读，
`chcp` 之后按 65001 重读，前段必须 ASCII 才不出乱码。
依据是本机已有可用先例：`桌面\video知识库剪贴板监控.bat`（UTF-8 + chcp 65001）。

**两条守卫**：① 被复制回项目目录（`%~f0` == `%WB%\启动工作台.bat`）时拒绝，
防无限自调用；② 项目路径不存在时给指引 + `exit /b 1`。

**生成与验证**（均由脚本完成，不手写）：
- `make_desktop_launcher.py`：生成 1826 B，回读校验（无 BOM / 55 CRLF / 0 裸 LF / chcp 在第 2 行）
- `e2e_desktop_launcher.py`：真实持久 PATH（**无受管运行时注入**）+ cwd=桌面 +
  BROWSER 打桩 → **8/8 PASS**（起服务、GET / 200、`Server: Workbench/1.0.10`、/api/securities、收尾释放端口）
- `neg_desktop_launcher.py` → **5/5 PASS**（自调用守卫 / 路径不存在提示 / 无重复探测逻辑 /
  委托仅一次 / 未污染正式文件）
- 桌面启动器 sha256 `e8ca90a47de731654cb3fce79bcbe9b9e8718c56a2722834f1aae4fab6b63478`

**本轮踩到的三个坑**：
1. **探针设计错**：把守卫测试做成"复制成别的文件名再跑"→ 守卫条件不满足，
   结果**真的启动了服务**（还差点开浏览器）。守卫要求的条件是**同名文件位于 WB 指向的目录**，
   故须用临时目录 + 字节替换 WB 来测。
2. **Bash heredoc 改写反斜杠**：heredoc 里的 `'C:\\Users\\...'` 到 Python 手里变成
   `C:/Users///u5b9c...`。探针改用 Write 落盘执行后正常。
3. **副作用：启动服务会改库**。e2e 验证跑完后 `data/workbench.db` 变 dirty——
   逐表逐列核对确认**只改了 `current_price` / `current_price_updated_at` 两列**（16 行全中），
   属应用正常的行情刷新。按"以云端版本为准"口径已 `git restore` 回仓库版本
   （核验：与 HEAD 逐字节一致、integrity ok、16/17/21/0/29/57 全在），并清掉 `-shm`/`-wal`。
