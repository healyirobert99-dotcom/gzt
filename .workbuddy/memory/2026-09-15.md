# 2026-09-15 工作日志

## 本轮任务：卡片右上角「×」归档 / 恢复（用户需求 = "删除按钮"）

用户原话：「给每个股票信息卡添加一个删除按钮，鼠标悬停后在右上角出现一个叉，可以删除这个股票」。
经 `AskUserQuestion` 拍板三条口径：

1. **删除语义 → 归档（软删除，可恢复）**：卡片从工作台消失，研究/计划/执行/台账全部原样保留，
   首页底部另有「已归档」入口可恢复。
2. **确认方式 → 自定义弹窗列出影响范围**：列出标的身份 + 研究/计划/执行/台账/流水各 N 条。
3. **版本节奏 → 只改代码 + 测试**，**不升版本号、不出交付包**（与 09-14「数据更新」按钮一致）。

### 为什么必须做成归档而不是 DELETE

五张子表（`research` / `trade_plans` / `execution_reviews` / `decision_ledger` / `trades`）
**全部 `ON DELETE RESTRICT` 引用 `securities(id)`**，且应用开着 `PRAGMA foreign_keys=ON`；
`decision_ledger` 是 append-only、必须保留"当时的判断"。实测 17 只标的**全部有历史**，
没有一只是"干净"的 → 任何 DELETE 都会永久抹掉历史。

## 设计

- `securities.archived_at TEXT` 是**可见性开关**，与交易状态 `status`（5 项白名单）**正交**，
  不是"第六种状态"。
- 归档 / 恢复各追加 1 条 `decision_ledger`（`event_type='标的归档'` / `'标的恢复'`），
  与「状态变更 / 研究更新 / 备注」的既有留痕约定一致。
- `list_securities(mode)` 三模式：`active`（默认，`archived_at IS NULL`）/ `only` / `all`，
  三模式自洽（active ∪ only == all，互不相交）。
- `GET /api/securities?archived=` 走**白名单** `_ARCHIVED_MODES`，非法值（`bogus`/`2`/`maybe`）
  一律 **400**，不静默兜底。

## 改动清单

**`app/server.py`**（+214 行）
- `SCHEMA.securities` 加 `archived_at TEXT`
- 新增 `EXPECTED_SECURITY_COLUMNS` + `_ensure_security_columns(conn)`：**不依赖
  schema_version** 的无条件幂等补列（在 `init_db` 里 `executescript(SCHEMA)` 之后立即调用）
- `_column_exists` 加 `hasattr(r,'keys')` 兼容分支（未设 `row_factory` 的连接按列名取值会抛
  `TypeError`，等于"启动即失败"）
- 迁移重建路径：`_expected_securities_cols`、重建 DDL、`INSERT ... SELECT` 复制列清单
  **三处同时**加 `archived_at`
- `list_securities(mode='active')`
- 归档域：`ARCHIVE_EVENT` / `UNARCHIVE_EVENT` / `_ARCHIVED_MODES` / `archive_impact()` /
  `_set_archived_at()`（**唯一写入口**）/ `archive_security_tx` / `unarchive_security_tx` /
  `archive_security` / `unarchive_security` / `list_archived_securities`
- 路由：`GET /api/securities?archived=`、`GET /api/securities/archived`、
  `GET /api/securities/{id}/archive-impact`、`POST /api/securities/{id}/archive`、
  `POST /api/securities/{id}/unarchive`

**`app/static/app.js`**（+112 行）
- `S.archived`；`loadAll()` 三请求并发（securities / archived / settings）
- `uiCardHtml` 卡片内加右上角 `.card-archive`（`onclick` **先** `stopPropagation` +
  `preventDefault` 再 `openArchiveModal`）
- `ARCHIVE_IMPACT_FIELDS` / `archiveImpactGrid` / `archiveTargetHtml` / `openArchiveModal` /
  `openRestoreModal` / `uiArchivedRow` / `uiArchivedSection`
- **键盘守卫**：`editing` → `onControl`，覆盖 `a[href], button, input, textarea, select,
  [contenteditable]`（否则 Enter 被页面快捷键吞掉，Tab 到 × 按 Enter 会跳详情页）

**`app/static/style.css`**（+48 行）
- `.terminal-card-head { padding-right: 24px }` 预留槽位防抖
- `.card-archive` 绝对定位 `top:9px/right:9px/z-index:3`，默认 `opacity:0;pointer-events:none`；
  `:hover` / `.keyboard-focus` / `.selected-card` 三态显形；`focus-visible` 可达
- 弹窗与「已归档」区样式

## 验证

| 层次 | 内容 | 结果 |
|---|---|---|
| 离线契约 | `tests/test_security_archive.py`（§A 语义 / §B 建表与补列 / §C HTTP / §D 前端静态） | **105/105** |
| 负向验证 | `.tmp_v108x/verify/neg_security_archive.py` 14 个回退变体 | **15/15**（含基线全绿 + `[BROKEN]` 检测） |
| 既有无回归 | 11 个套件全绿 | **665 断言** |
| 真实浏览器 | `.tmp_v108x/verify/browser_e2e_archive.py`（真实服务 8805 + 真实 Chromium） | **38/38** |

浏览器 E2E 的关键实测证据：
- 未悬停 `opacity: 0 / pointer-events: none` → 悬停后 `opacity: 1 / pointer-events: auto`，
  `absolute / top 9px / right 9px / z-index 3`，20×20
- 归档弹窗：宁德时代 300750·SZ 持仓中，研究 2 / 计划 4 / 执行 6 / 台账 14 / 流水 0，
  「共 26 条记录不会被删除」，含"持仓中 → 不改变状态"警示
- 确认后：`POST /api/securities/{id}/archive` → 200，卡片 17→16，子表逐表不变，台账 +1
- 已归档区：1 个标的、`研究 v2 · 计划 v4 · 执行 2026-09-15 · 归档于 …`、可恢复
- 恢复弹窗：台账显示 **15**（= 归档前 14 + 1 条归档留痕）——append-only 的直接实证
- 往返净零：归档归零、17 只全部可见、`archived_at` 归 NULL、`status` 不变
- **真实库 `data/workbench.db` 全程 SHA-256 逐字节未变**（`4f4832aa51d00b51…`）

证据截图：`output/20260915-archive-btn/01-归档确认弹窗.png`、`02-已归档区展开.png`、
`03-恢复确认弹窗.png`。

## 本轮新发现的环境陷阱（已写入 `ENV-TRAPS.md`）

agent-browser 五条会**伪装成产品 bug** 的坑：
1. **带空格的复合选择器被截断成第一个 token** —— `click ".mfoot button.primary"` 实际点的是
   外层 `.mfoot` div，却照样报 `✓ Done`。→ 一律用无空格 `>` 子选择器。
2. **`click` 不自动滚动进视口** —— 视口外的元素点击静默落空（首页「已归档」区在绝对
   y≈3381）。→ 先 `set viewport 1280 4000`。
3. **会话跑久了 `:hover` 失效** —— `get styles` 恒读默认态，悬停断言假红；而同一批次的
   `click` 仍成功（Playwright 给元素自己补 hover）。→ `agent-browser close --all` 重开。
4. **`✓ Done` 只代表命令已派发，不代表点中了元素**。
5. `get text` **取不到折叠 `<details>` 里的内容** → 先展开再读。

Python 驱动 agent-browser 的两条：
6. `agent-browser` 是 sh 垫片，`subprocess` 直接调它 → `FileNotFoundError [WinError 2]`。
   须直调 `<node.exe> node_modules/agent-browser/bin/agent-browser.js`。
7. **`subprocess.run(capture_output=True, timeout=)` 会永久挂死**：超时只 kill node.exe，
   派生进程仍持有 stdout 管道 → `communicate()` 等不到 EOF，实测无声挂了 **20 分钟**。
   → 输出重定向到文件（不接管道）+ `Popen` + 超时 `taskkill /F /T`。

## 另一个教训（脚本而非产品）

首页卡片排序**依赖行情**，`.terminal-card` 的"第一张"跨批次不稳定（实测两次相邻批次点到
亿联网络 300628.SZ 与 宁德时代 300750.SZ）。第一版 E2E 按 DOM 顺序猜标的身份，结果把 §3 的
断言打到了另一只股票上 —— 全红但产品完全正常。**改为「点 × → 读弹窗 → 确认归档」在同一批次
内完成，标的身份一律以库里归档结果为准。**

## 收尾

- 沙箱服务（8805）、`.tmp_v108x/verify/sandbox_arch/`、浏览器会话均已清理；探针噪声文件已删。
- 版本号 4 处 + `app.js` 头部 **均未改动**（仍 v1.0.10），未出交付包。
- 真实库 `data/workbench.db` 的改动属用户自己的数据演进（16→17 只、台账增长），非本轮所致；
  本轮全程只操作副本。

## 收尾后的边界检查（同日，用户指示"补做边界检查"）

对刚交付的归档功能补查**自身引入的路径** —— 不加新功能，只查已有实现会不会塌。
新增 `.tmp_v108x/verify/browser_e2e_archive_edge.py`（**28 断言**，同样在沙箱 8805 上可重复跑）：

| 路径 | 实测结论 |
|---|---|
| **归档到只剩 0 张卡** | 首页完整可用：五个分组全落「暂无标的」空态（`0 个标的`×5 处）、`.terminal-card`/`.card-archive` 均为 0、**未进 catch 分支**；「已归档」区正常渲染并展开列出全部 17 行 |
| **后退进入已归档标的 `#/s/{id}`** | 详情抽屉照常打开（`.detail-drawer`=1，含名称/代码与「更新动态执行」入口）；详情页背后的首页确实 0 张卡 —— 证实 `openDetailDrawer` 走独立接口、**不依赖 `S.secs`** |
| **已归档标的的不可见性** | 默认接口与「标的库」页均不含（`共 0 个标的` + 空态指引） |
| **全归档仍可逆** | 从「已归档」区点恢复 → 首页由 0 回到 1 张卡；存量全恢复回 17 只；台账恰 +2×17 |
| **隔离性** | 真实库 SHA-256 逐字节未变 |

**结论：无产品缺陷** —— 归档后工作台在极端态下仍完整可用，且严格可逆。

### 新踩的环境陷阱（已补进 `ENV-TRAPS.md` ⑪）

**一次 batch 里多条命令的输出会被合并进同一个块，`✓ Done` 并非每命令一枚。**
探针 `probe_count.py` 实测：3 个 `get count` + 1 个 `get text` 的输出挤成一块
`'\n\n17\n\n0\n\n06:10\n\n17\n'`。于是 `browser_e2e_archive.py` 里 `ints()` / `last_int()`
（假设"每命令一块、块内容恰好是数字"）**只在 count 恰好在批次末尾时才侥幸正确**。

首轮边界脚本因此 **4 条假红**（`card=17` / `btn=None` / `drawer=None` / `card=None`），
而**同一批次的文本证据全 PASS** —— 极具迷惑性，差点误判成"归档到 0 张卡页面就坏了"。

**正解**：`get count` **独占一个批次**（不与 `get text` 混，否则文本里的纯整数行会污染读数），
再**按行**收纯整数行（`0` 也会照常输出，能区分"一个都没有"与"读数失败"）。
已提为公共 helper `browser_e2e_archive.py::count_values()`，两份脚本共用。
修正后：边界 **28/28**、E2E 回归 **38/38**（**未改动任何既有断言**）。

## 边界检查发现的**真缺陷**：归档 × A/H 关联（已修）

把边界检查再往外推一层 —— 查「归档会不会牵连别的字段」—— **揪出一条真缺陷**，
且正是归档功能自己引入的回归。

### 症状

A 关联 B（`securities.ah_link_id`）。把 **B 归档**后，打开 A 的「编辑基本信息」，
**只改行业/备注再点保存**，A→B 的关联就被**静默清空**（`ah_link_id` 写成 NULL），
提示仍是「基本信息已更新」—— 用户完全无从察觉。

### 根因

`openBasicModal` 里 A/H 下拉的候选项来自 `S.secs.filter(...)`，而 **`S.secs` 不含已归档标的**
→ 下拉里没有 B → 浏览器回落到首项「— 无 —」→ 提交 `ah_link_id=""` →
后端 `ah in (None,'') → None` 写回 NULL。
后端本身**不校验归档状态**（允许关联已归档标的），所以问题纯在前端的候选集构造。

### 修法（最小改动，两处同一口径）

- `openBasicModal`：候选里**补回被归档的关联项**并标注「· 已归档」；**仅在"当前已关联
  且该关联不在活跃列表"时补**（不无条件把已归档标的塞进下拉）。
- `rowHtml`（标的库）：A/H 关联显示同样回退到 `S.archived`，并附「（已归档）」，
  否则关联说明会凭空消失。

### 验证（四级）

| 层次 | 内容 | 结果 |
|---|---|---|
| 端到端**负向** | `check_ah_link_archive.py`（真实 8805 + 真实 Chromium：抽屉 → ··· 更多 → 编辑基本信息 → 保存） | **修正前 7/11（4 红）→ 修正后 11/11** |
| 离线契约 | `test_security_archive.py` 新增 **§D#9 共 5 条** | **110/110** |
| 全套回归 | 11 套件一键汇总（`run_all_tests.py`） | **670 PASS / 0 FAIL**（665 → 670，净增 5 = §D#9），逐套与基线一致 |
| 浏览器回归 | E2E 38/38、边界 28/28 | 全绿 |

修正前 4 条红里有 **3 条是决定性证据**：
1. 下拉选项数 **16**（应为 17）—— 说明 B 根本没进候选；
2. 下拉文本里**没有**「安踏体育」；
3. 保存后 `ah_link_id` 变成 **None** —— 静默清空的直接实证。
第 4 条是第 3 条的连带后果。

### 顺带修掉的**测试装置**缺陷

首版脚本 §3#2/§3#3 误报「Element not found」：上一批次把弹窗留在打开状态，下一批次的
`open` 之后抽屉被遮罩挡住，点击链静默落空。对策：`modal_open_batch()` 每次先 `close_all()`
重开会话。
**教训：假红先证伪"读数/装置"，再怀疑产品** —— 本轮两次假红（batch 输出合并、弹窗未关）
都是装置问题，产品一次都没坏。

### 新增工具

- `.tmp_v108x/verify/run_all_tests.py`：一键跑 11 套件 + **与已知基线交叉核对**。
  各套件汇总格式互不统一（`v103` 只打「全部通过」且断言用 `[✅]`；`v106/107/108` 只打
  `FAIL_COUNT`），故用「优先取汇总行、否则数断言行」两级解析 —— 与基线全对得上才敢下结论。
- `.gitignore` 追加忽略 `sandbox_arch/`（真实库副本 + app 副本，纯临时）。

## 同一根因的第二处：已归档标的的详情页是"死路"（已修）

修完 A/H 关联后，按技能里新立的规则 —— **"凡某集合被隐藏/过滤后，别处仍有表单或缓存
依赖它，都要逐个查一遍"** —— 把 `S.secs` 的**全部消费点**清了类：

| 消费点 | 性质 | 结论 |
|---|---|---|
| `findSec`（7 个操作入口共用） | **操作入口** | **缺陷**（见下） |
| `openBasicModal` 的 A/H 下拉 | **表单** | 上一节已修 |
| `rowHtml` 的 A/H 关联显示 | 展示 | 上一节已修 |
| `refreshQuotes` 的 symbols | 只给活跃标的刷行情 | 合理（隐藏标的不需要行情） |
| 首页分组 / `uiLifecycleSort` / `attention` / 顶部统计 | 展示 | 正确（归档即从工作台消失） |
| `renderList` 计数与行 | 展示 | 正确 |
| 命令面板（Ctrl+K）下拉 | 导航 | 见下方"开放项" |
| 6 个 `<select>` 中除 A/H 外的 5 个（交易所/状态/方向/验证项状态…） | **固定枚举** | 无风险 |

**关键结论：全项目只有 1 个下拉是由标的列表动态生成的（就是 A/H），其余 5 个都是固定
枚举** —— 所以"静默清空"这类缺陷只有那一处实例，已修完。

### 缺陷：`#/s/{id}` 打开已归档标的时，4 个操作按钮全部静默失效

`const findSec = id => S.secs.find(...)`，而 `S.secs` 不含已归档标的。于是从 `#/s/{id}`
（书签 / 浏览器后退 / **标的库的 A/H 关联链接**，正是上一节新加的那条）进入已归档标的时，
详情抽屉整张渲染完整，但操作区：

- ↗ 更新动态执行 → `openExecutionModal` → `findSec` 返回 undefined → **静默 return**
- ⇄ 录入交易 / ⌁ 查看研究 → 同样
- ··· 更多 → `openMoreActions` **不查 findSec**，弹窗能开；但它里面 4 个按钮又都过 findSec
  → 全部静默

**点了毫无反应，且没有任何提示。**

### 判据：这是缺陷，不是"只读设计"

- 项目自己的设计记录写着 `archived_at` 是**可见性开关**（与交易状态 `status` **正交**）；
- 后端里 `archived_at` 只出现在三处 —— schema、列表过滤、归档/恢复写入 ——
  **没有任何 mutation 端点校验它**，即后端本就允许编辑已归档标的；
- 所以前端是把"可见性"当成了"只读"，而且失败得**静默**（项目铁律：不得静默失败）。

### 修法（一行）

```js
const findSec = id => S.secs.find(s => s.id === Number(id))
  || (S.archived || []).find(s => s.id === Number(id));
```

### 验证（四级）

| 层次 | 内容 | 结果 |
|---|---|---|
| 端到端**负向** | 边界脚本新增 **§E7**（已归档标的详情页点 3 个操作入口 + 1 个对照组，真实 Chromium） | **修正前 29/32（§E7#1~#3 红）→ 修正后 32/32** |
| **对照组** | §E7#4「··· 更多」不走 `findSec` → 修正前也是绿的 | 恰好证明差异**就是那道闸门** |
| 离线契约 | `test_security_archive.py` 新增 **§D#10 共 3 条** | 套件 **113/113** |
| 全套回归 | 11 套件一键汇总 | **673 PASS / 0 FAIL**，逐套与基线一致 |
| 浏览器回归 | E2E 38/38、A/H 11/11 | 全绿；真实库 SHA-256 三份一致、与会话开始时相同 |

### 开放项（不擅自决定）

**命令面板（Ctrl+K）搜不到已归档标的。** 这是"归档 = 从工作台消失"的直接后果，
但也会造成"我知道它存在却搜不到"。两条路：① 保持现状（用首页底部「已归档」区找）；
② 把已归档标的也纳入搜索并标注「已归档」。**留待用户拍板。**

---

## 边界检查收尾（第三、四个同型面）：导入流程 × 归档

「被过滤集合」检查法还剩两个服务端同型面，本回合查完。**结论：数据完整性全对，
交互文案有两处缺口（已记录、未擅自改）。**

### 做法（进程内探针，不起服务）

新增 `.tmp_v108x/verify/check_archive_import.py`：把真实库复制到临时目录，
monkeypatch `server.DB_PATH`，**在进程内**直接调 `server.py` 的公开函数，
**真实库一个字节都不碰**（SHA-256 前后一致，`4f4832aa51d00b51…`）。
覆盖「新建 → 归档 → 格式 A 全量导入 → 格式 B 执行导入 → 重复创建 → 隔离性」，**38 断言**。

### 关键结论

- 导入按 `exchange+code` 匹配、**不按归档过滤** —— 这是**正确**的：身份只认
  `exchange+code`；若按归档过滤，归档标的就再也导不进来了。
- `is_new = row is None` ⇒ 归档标的走 UPDATE 路径，**不会**被当成新标的重新 INSERT。
- 导入**不复活归档**：`archived_at` 保持非空、时间戳未被改写、不伪造归档/恢复台账事件；
  导入 7 个函数体内**完全不出现 `archived_at`**（既不读也不写）。
- 顺带验证：**仓库版 DB 快照曾经没有 `archived_at` 列**（快照早于该功能），而 `init_db`
  的**无条件幂等补列**能在 `do_migration` **被整体跳过**的情况下把列加上 —— 这正是
  v1.0.10 那条缺陷登记所依赖的路径，现在有进程内证据了。

### 两处交互缺口（**只记录，不擅自改**）

1. `preview_import_full` 的载荷不带 `is_archived`：用户从预览里**看不出**"该标的已归档，
   导入后仍然隐藏"，提交成功了卡片却不出现，容易被误判成导入失败。
2. 对已归档标的再次「创建」只报「该交易所已存在相同代码…禁止重复创建。」，未提示
   "该标的是**已归档**而非已删除，可到首页「已归档」区恢复"。

两条都只影响**文案**，数据完整性已验证无误；改文案属功能改动，**留待用户拍板**。

### 第二个面：归档标的的行情展示 —— 判定为**合理，无缺陷**

`refreshQuotes` 只对 `S.secs`（活跃）拉行情 ⇒ 已归档标的 `quoteOf()` 返回 null。
逐一核查 6 个渲染点：`uiMiniChart` 首行即判空返回占位符，其余 5 处都有「暂无行情」
三目兜底 ⇒ **不会渲染出 undefined/NaN**，抽屉也不会崩。属展示类，正确。

### 本轮加固（新增契约 + 负向验证）

- `test_security_archive.py` 新增 **§D#11 共 6 条**（归档 × 导入路径）+ **§D#12 共 3 条**
  （行情兜底）⇒ 套件 **113 → 122**；全套 **673 → 682 PASS / 0 FAIL**，逐套与基线一致。
- 新增负向验证 `.tmp_v108x/verify/neg_archive_import_contract.py`：把仓库复制到临时目录，
  注入 4 个"真实会出问题"的改动（匹配查询加 `AND archived_at IS NULL`、
  `_import_apply_one` 读 `archived_at`、造第二个 `archived_at` 写入口、删掉「暂无行情」兜底），
  **4 次注入全部按预期翻红（8/8）**，证明新断言不是恒真。
- 浏览器三套回归：E2E **38/38**、边界 **32/32**、A/H **11/11**；三份证据的真实库 SHA-256
  与会话开始时相同。

### 收尾

- 记忆整理：`MEMORY.md` 被注入时超长截断 ⇒ 拆出 `DEFECTS.md`（缺陷细化登记），
  MEMORY.md 由 13.8 KB 压到 10.5 KB，零信息丢失。
- 端口清空：8805 已按端口属主杀进程树，`sandbox_arch/` 已删，8765 无遗留监听。
- **未升版本号**（用户拍板"只改代码 + 测试"），故不出交付包。


---

## 第五个同型面收尾：「··· 更多」转发器与全项目消费点闭口

前四轮把 `S.secs`（活跃列表）被误用的两处揪出并修掉。本轮把**最后一个未查的消费点**
补完，并把「同一个 id 从另一个入口进来」这一支线闭合。

### 新查明的机制：抽屉上「··· 更多」是**纯 id 转发器**

`openMoreActions(id)` 自己**不查标的**，只把 `id` 原样塞进 4 个 `onclick`
（`closeModal();openStatusModal(${id})` …），由各弹窗内部走 `findSec`。于是：
只要转发器保持纯透传，已归档标的的 4 项就天然可用；**一旦有人图省事在转发器里先查一次
活跃列表**（`const s = S.secs.find(...); if (!s) return;`），4 项会**一起**静默失效。
这条不变式此前无任何测试覆盖 —— 本轮补上。

### 边界脚本 §E7 扩到 8 条 + 新增 §E8 / §E9（32 → **37 断言**）

- **§E7**（4 条，已归档标的详情页 4 个入口）+ 对照组「··· 更多」。
- **§E8**（4 条，**新增**）：「··· 更多」菜单**打开后**再点里面 4 项 —— 打开菜单 ≠ 点中菜单项。
  判据取各弹窗**独有**的字段标签（新状态 / 修改说明（记入台账） / A/H 两地上市关联 / 记录内容（必填）），
  避免随便弹个窗就算过的假绿。
- **§E9**（1 条，**新增**）：同一条点击链在**活跃**标的上同样成立 —— 排除选择器碰巧命中。

### 离线契约 §D#10d / §D#10e（122 → **124 断言**）

- `§D#10d`：`openMoreActions` 函数体内**不出现** `S.secs`/`findSec`，且只有 1 次 `openModal(`。
- `§D#10e`：4 个 `onclick` 都是 `?${id}`（4/4），无一被换成对象。

### 负向验证（两处，全部按预期翻红）

- `neg_archive_import_contract.py` 扩到 **6 次注入**（新增 M5：转发器里加活跃列表查找；
  M6：把某一项 `${id}` 换成 `${s.id}`）⇒ **12/12**，6 次注入的目标断言均翻红，且副本仍能跑到汇总行。
- `neg_findsec_browser.py`（**浏览器层**，6 断言）：把沙箱里的 `findSec` 回退成单级查找，
  重跑边界脚本 ⇒ 目标 **7 条**（§E7#1~#3 + §E8#1~#4）**全部翻红**，而对照组 §E7#4（不走 findSec）
  与 §E9#1（活跃标的）**保持绿** ⇒ 排除了整体坏了这一混淆解释。

### 全项目 `S.secs` 消费点最终结论（闭口）

17 个消费点逐条判过，**只有 2 处构成缺陷**（A/H 下拉静默清空、`findSec` 静默失效），均已修。
其余：`uiRiskCount` / 分组 / 排序 / 标的库表格 / 命令面板 —— 都是渲染哪些卡片或展示类，
对已归档标的本就**应当**不出现。特别核对两处易误判的：
- `uiLifecycleSort` 用 `S.secs.indexOf`（返回 -1 会排首位）—— 但入参恒来自 `S.secs` 自身，
  永不 -1，**无缺陷**。
- 前端无全局台账页/交易页（路由只有 home / list / import / new / detail），
  台账与流水都在**详情抽屉**里、走真实详情接口 `/api/securities/{id}`，**不受归档影响**。

### 一条通用判据（新增到检查法）

**转发器也是操作入口**：若某菜单/中间层只做 `id` 透传，则它**不能**自行过滤集合 ——
过滤一旦加在中转层，下游所有被转发项会**同时**失效，且各下游自身的 `findSec` 全绿，
从代码上看不出问题。故对每个中转函数都要问："它有没有偷偷查列表？"

### 本轮数字

- 离线全套 **682 → 684 PASS / 0 FAIL**，11 套件逐套与基线一致。
- 浏览器：E2E **38/38**、边界 **37/37**、A/H **11/11**；进程内 `check_archive_import.py` **38/38**。
- 真实库 `data/workbench.db` SHA-256 全程 `4f4832aa51d00b51…`（与会话开始时相同）。
- **未升版本号**，仍未出交付包；`8765 / 8805 / 8799` 全空闲。


---

## 按铁律补跑并发探针：归档 / 恢复 × append-only 台账

前几轮做了语义、导入、行情、转发器四个面，**唯独漏了项目自己写下的那条铁律**：
「凡 append-only 写入的改动必须跑并发探针」。归档/恢复每次追加 1 条 `decision_ledger`，
正是这一类；而 `_set_archived_at` 又是「**读校验 → 写状态 → 追加台账**」三段式，
与 v1.0.9 漏出的 R-027 是同一个形状。顺序路径全绿完全不能证明它没问题。

### 结论：**并发安全，无缺陷** —— 但现在有证据了

新增 `.tmp_v108x/verify/concurrency_archive_probe.py`（**12 断言**）：

- §1 纯归档：**12 轮 × 8 线程**打同一标的 → changed=True **严格 == 1/轮**，
  台账增量 == changed 数，无幽灵行、无其它异常、终态必为已归档。
- §2 混合：12 轮 × 8 线程归档/恢复交替 → 两类台账增量各等于对应方向的成功数；
  终态与成功次数**奇偶守恒**（每次 changed=True 都是一次真翻转）。
- 192 次调用里真的撞上 `SQLITE_BUSY` **32 次（约 17%）**，全部按受控冲突处理。

顺带把这条固化成**离线契约** `§C#15~§C#15d`（4 条，真实 `ThreadingHTTPServer` +
真 HTTP 并发）：每轮 changed=True 恰好 1 个 / 台账增量 == changed 数 /
状态码 ∈ {200,409} 绝不出现 500 / 终态已归档。

### 一个容易退回的细节

`_is_db_busy_error` 用 `code & 0xFF` 取**主码**，所以 `SQLITE_BUSY_SNAPSHOT`(517)
也被正确归入 409 族。若有人"简化"成只比对 5/6，WAL 下的快照升级失败就会变成 **500**。
探针里那 32 次 BUSY 正是这条路径的真实来源。

### 探针自身的 bug（已修，值得记）

首跑 4 条 FAIL，差点当成产品缺陷。实为**探针 delta 算错**：把
`other = (all_now - base[ARCH] - base[UNARCH]) - base[other]` 写成了等价于
**总增量**的式子，于是每轮出现"幽灵 +1"。改成先算三个 delta 再相减即全绿。
**教训：探针报红的第一步永远是怀疑探针自己**（这已是本会话第二次）。

### 负向验证（新增 M7 / M8）

- **M7** 去掉 `_set_archived_at` 的幂等守卫 → 单轮 6 个 changed=True、并出现 409 ⇒ §C#15 红。
- **M8** 写了台账却返回 `changed=False`（幽灵行）⇒ §C#15b 红。
- 8 次注入合计 **16/16 PASS**，次次命中目标断言。

### 本轮数字

- 离线全套 **684 → 688 PASS / 0 FAIL**（+4 = §C#15~§C#15d），11 套件逐套与基线一致。
- 浏览器：E2E **38/38**、边界 **37/37**、A/H **11/11**；进程内 `check_archive_import.py` **38/38**；
  并发 `concurrency_archive_probe.py` **12/12**。
- 真实库 SHA-256 全程 `4f4832aa51d00b51…`；`8765/8805/8799` 全空闲；未升版本号。
