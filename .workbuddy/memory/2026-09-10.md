# 2026-09-10

## A/H 个股投研交易工作台 MVP 落地（D:\个股工作台）

按用户需求文档（11 节结构化 spec）完成第一版 MVP，技术决策经用户确认：
- 架构：Python 3.13 标准库（零第三方依赖）+ SQLite（data/workbench.db, WAL）+ 浏览器 SPA
- 行情：腾讯免费接口 qt.gtimg.cn 服务端代理（GBK 解码，8s 缓存），只提供客观价格，不生成建议
- 示例数据：美图公司（01357.HK, HKD）+ 道通科技（688208.SH, CNY）预置

文件结构：
- app/server.py —— HTTP 服务 + REST API + schema + 种子数据 + 行情代理
- app/static/{index.html, app.js, style.css} —— 前端（hash 路由 SPA，深色主题，涨红跌绿）
- 启动工作台.bat —— GBK 编码（含中文 Python 路径必须 GBK，UTF-8 会乱码）
- 端口 127.0.0.1:8765

数据模型四层：securities（标的+交易状态）/ research（版本化研究结论）/ trade_plans（版本化交易计划）/ trades（append-only 流水，持仓由流水推导）；decision_ledger 记录状态变更（原因必填）、计划/研究版本、交易、备注。

踩坑记录：
1. 工具链会把命令/文件中的字面 `C:\Users\...` 反斜杠规范化为正斜杠 —— 生成 bat 需用 Python os.path.join 运行时拼接规避
2. bat 含中文路径必须 GBK 编码写入（cmd 默认 936 代码页）
3. 腾讯行情返回 GBK，需 .decode('gbk','ignore')

已修复 bug：get_detail 原返回扁平结构，前端期望 d.security 嵌套 → 改为显式 {security, research, plan, position, histories, trades, ledger}。

全接口回归通过（状态变更原因必填校验、卖出超持仓拦截、计划版本化、静态资源、路径穿越 404）。

## 生产交付审计文档（A-H-WORKBENCH-MVP v1.0.0）

用户提出"第三方审计"用途的正式交付文档。通过 tdoc-orchestrator 流水线 full_pipeline 模式编排：
- 路由：交付审计文档未命中 L2 专家，→ general-writer (doc-writer 直产 HTML)
- Stage 1 (doc-writer)：产 `output/20260910-audit/stage2/intermediate/output.html`，按 design-token 商务报告风格：深蓝 #1F3A68 主色 + 章节横线 + 表格表头底色 + callout (warn/danger/ok)
- Stage 2 (doc-typeset 等价)：HTML 中已内嵌商务报告样式 CSS（设计令牌直接 inline 注入，节省 LLM 调用以避开上次的 429 限流）
- Stage 3 (html-to-docx 等价)：原 html-to-docx 包依赖 click+多第三方且 CLI 调用复杂，本次降级用 python-docx 1.2.0（已 pip 到 default venv）直接生成 docx —— 因内容与版式 100% 确定、由 python-docx 生成可避免外部依赖并保证表格/边框/底色完全可控

最终交付物：`D:\个股工作台\output\20260910-audit\stage3\A-H投研交易工作台交付审计文档.docx`
- 57,834 bytes
- SHA-256: 2c06d636d83c4cc6588f658f9e3f49231452b8405960df74a7f260408a72cb30
- 141 段落 / 29 表格 / 13 节（封面 + 目录 + 12 章正文 + 附录 + 签字栏）

文档结构（13 节）：封面 → 目录 → §1 文档元信息 → §2 项目目标与边界（含明确不做清单） → §3 架构 → §4 交付物清单（含全部 SHA-256） → §5 完整 SQLite schema DDL → §6 REST API 规格 → §7 前端 → §8 安全审计要点 → §9 测试报告与回归证据 → §10 部署运维 → §11 风险登记册 → §12 第三方审计重点 → §13 附录源码 + 签字栏。

踩坑补充：
1. python-docx 在 default venv 中原本未安装，需先 `pip install python-docx`（pip 路径: `C:\Users\宜春法院\.workbuddy\binaries\python\envs\default\Scripts\python.exe -m pip install python-docx`）
2. add_callout 中曾误用 walrus 操作符（`cells:=0` + row 下标），导致 '_Row' object is not subscriptable；改为直接 table.rows[0].cells[0]
3. python-docx 生成 docx 表头底色需 OxmlElement 注入 w:shd，否则默认无背景
4. 上次遭遇 429 频率限制，本次主动合并 Stage1+2 的 LLM 调用，写脚本化生产，节省 token

## A/H 投研交易工作台 v1.0.1（数据准确性修复）

第二轮独立代码审计后，14 项 v1.0.1 修复：
- SQLite FOREIGN KEY + UNIQUE(exchange, code) + CHECK + PRAGMA foreign_keys=ON
- 多币种 HKD→CNY 缺汇率显式 None（不输出错误比例）
- 行情按市场字段区分（A/港 f[30] 时间格式不同）
- 业务写入 + ledger 同事务，失败整体 ROLLBACK（_LEDGER_FAIL_INJECT 测试钩子）
- /api/quotes 同时返回 last_success_at / last_error（前端缓存旧价语义明确"上次成功行情"）
- 持仓/状态一致性提示（不自动改 status）
- 迁移函数 migrate_v101 + 在线 Backup API（sqlite3.Connection.backup）
- 删除生产默认 seed（美图/道通移出生产 DB → fixture）

交付物：`output/20260910-audit/stage3/A-H投研交易工作台交付审计文档-v1.0.1.docx`

## A/H 投研交易工作台 v1.0.2（修复第二轮独立代码审计）

按用户指令严格修复（v1.0.2 = 修复版），边界："未经用户明确批准，不得新增规则、扩展任务或改变研究口径"。

**13 类关键修复（FIX-14 ~ FIX-32）**：

- **行情→持仓链路真实打通**：成功拉到的行情自动回写 `securities.current_price / current_price_updated_at`（`_persist_quote_to_securities`）。compute_position 直接读取最新价。
- **状态弹窗修复**：openStatusModal option 拼接（`value="${esc(x)}"${x === s.status ? ' selected' : ''}`），避免"可交易"成为无意默认。
- **历史补录校验**：trade_date 强制 ISO YYYY-MM-DD（datetime.strptime）；写入前完整时序累计 ≥0 校验；首笔不能为卖出；删除 v1.0.1 的 `q=min(q, qty)` 静默截断。
- **生产默认假数据清空**：迁移 `UPDATE settings SET value='' WHERE key IN ('hkd_cny_rate','account_size_cny')`；前端删除 `|| '0.92'` 默认。
- **测试隔离**：test_v102.py 全部用 tempfile 临时 DB + Proxy monkey-patch server.get_db；生产 DB 哈希 0 改变（测试末尾实时校核）。
- **真实腾讯接口拆分**：`tests/test_integration_quote.py` 单独跑；网络不可达整体跳过；不影响本地测试。
- **Migration 原子化 + 幂等**：
  - 启动先读 settings.schema_version，等于 TARGET_SCHEMA_VERSION 完全跳过 do_migration
  - do_migration: backup → BEGIN → _migrate_v101_minimal + migrate_v102 → CREATE INDEX 4 个 → _verify_indexes → FK check → COMMIT；任意失败整体 ROLLBACK
  - 所有 executescript 改为 conn.execute 逐条（避免 SQLite 隐式 COMMIT 破坏外层事务）
  - UNIQUE 检测改用 PRAGMA index_list + index_info（不再依赖 sqlite_master sql 文本）
  - 旧库可能缺列（ah_link_id 等），用 ALTER TABLE 补齐
- **数据库级版本唯一性**：research / trade_plans 加 `UNIQUE(security_id, version)`；迁移前重复版本号检查，发现抛 MigrationError，不擅自合并。
- **计划 change_note 必填**：PUT /plan 缺 change_note 拒绝（首次除外）。
- **行情失败语义**：从未成功时 error 必填；缓存旧价可展示但语义明确"上次成功行情"；价格派生标签带时间戳（不伪装为"今天的事实"）。
- **sampleBanner 改写**：showSampleNoticeIfFixturePresent() —— 仅当 securities 含美图/道通时才显示；真实用户创建第一只股票不再被强制看到"示例数据"提示。
- **重复启动器删除**：app/启动工作台.bat（cd 到 app 后跑 app/app/server.py） → 删除，仅留根目录启动器。
- **document 修正**：
  - design_research_pool_history.md 改事实一致（research 表已有该列；securities.research_pool 仅首次写入后不再改）
  - design_reversal.md：本轮不实施（旧"反向真实 trade"污染 realized_pnl）
  - audit §8 安全描述：修正为"动态用户内容经 esc 转义后插入；可点击 URL 经 sanitizeUrl 白名单过滤 javascript:/data: 等"
  - 404 vs 400：新建 NotFoundError 异常类；do_GET / _mut 分别捕获；不存在标的必为 404
- **前端细节**：sanitizeUrl 过滤 javascript: 等危险 scheme；report_link a.href 与 rel="noopener noreferrer"；港股/CNY 单位每个字段独立标注；折算市值标 CNY + 汇率。
- **--seed sys import 修复**：server.py 顶部加 `import sys`（init_db 内有 `sys.path.insert(...)`）。

**关键踩坑（迁移/测试路径）**：
1. `sqlite3.executescript` 会隐式 COMMIT，把外层 BEGIN 干掉——所有迁移内 rebuild 改用 conn.execute 逐条
2. 旧 v1.0.0 库列结构与现代不同——ALTER TABLE 补全 securities 所有期望列后再重建
3. test_v102 §L 段 `srv` 名称被复用了 ThreadingHTTPServer 实例，注入失败改用 `server._LEDGER_FAIL_INJECT`（模块级引用）
4. 整合 compute_position 时 HKD fx_rate_missing 必须独立判断（不依赖 cur_price）—— 当无当前价但 HKD 时仍标记 fx_rate_missing=True
5. v1.0.1 旧测试 test_v101.py 直接读生产 DB，违反 v1.0.2 隔离要求——已删除并 archive 到 archive/v101/

**测试结果**：
- tests/test_v102.py —— 38/38 通过（14 节覆盖）
- tests/test_integration_quote.py —— 14/14 通过（含真实腾讯接口：A 股 sh688208 25.54、港股 hk01357 4.24）

**交付物**：
- `output/20260910-audit/stage3/A-H投研交易工作台交付审计文档-v1.0.2.docx` (46803 B)
- `个股工作台-v1.0.2-20260910.zip` (273.1 KB, 21 文件)
- SHA-256 见审计文档 §4

**仍未解决**：
- O-003: trades 冲正机制（旧方案污染 realized_pnl，本轮不实施；design_reversal.md 列 A/B/C 三个未来方向待批准）
- O-004: securities.research_pool 字段彻底废弃（SQLite 不支持 DROP COLUMN，本轮不擅自 DROP；design_research_pool_history.md 已列事实状态）
- R-014 / R-015 / R-016 / R-017：边界外的风险（单一行情源、缺汇率仓位、迁移长时间事务并发、补录错误回滚机制）

---

## v1.0.3 增量（用户明确批准的新模块：「动态执行层」）

**本轮性质**：用户明确批准的功能扩展（非"修复"），与 v1.0.2 修复轮性质不同。

**核心边界**（已严格遵守）：
- 不修改研究结论/静态交易计划/状态体系/工作流
- 不引入 MA/EMA/MACD/RSI/KDJ/布林带/成交量评分/技术评分/自动支撑位/自动择时/买卖信号/止损/仓位模型/状态机/股票评分
- execution_view 只由用户手动录入；行情刷新不得改写
- 不建立新自动状态机

**新增内容**：
- 表 `execution_reviews`（append-only，FK → securities，5 字段索引 idx_exec_review）
- 常量 `EXECUTION_VIEWS = [等待技术确认, 可以开始执行, 暂缓执行, 继续观察]`
- 函数 `add_execution_tx` / `get_execution`（同事务写 execution_reviews + decision_ledger）
- 端点 `GET/POST /api/securities/{id}/execution`
- `do_migration` 新增 `migrate_v103`；备份文件名改为 `workbench-pre-v103-*.db`
- `_verify_indexes` 现在验证 5 个索引（含 idx_exec_review）

**关键踩坑（v1.0.3 新增）**：
1. `@_run_in_transaction` 装饰器定义在文件中段（line ~1360）；`add_execution_tx` 必须放装饰器之后才合法——最初放装饰器前导致 NameError
2. `ledger_add` 之前未挂 `_LEDGER_FAIL_INJECT` 钩子，需在函数体内补 `if _LEDGER_FAIL_INJECT: raise RuntimeError(...)`，否则事务回滚测试无法触发
3. `_norm_code` 严格要求 HK 5 位 / A 股 6 位数字——test_v103.py 最初用的 "900001" 不符合 HK 规则，需改为 "00001"

**测试结果**：
- tests/test_v102.py —— 38/38 通过（更新 schema_version 期望为 1.0.3）
- tests/test_v103.py —— 35/35 通过（7 节：新增可读/旧保留/事务回滚/不改 plan/不改 quote/首页双显/无 record 提示）
- tests/test_integration_quote.py —— 14/14（独立跑）

**交付物**：
- `output/20260910-audit/stage3/A-H投研交易工作台交付审计文档-v1.0.3.docx` (46264 B, SHA-256 b268e012...)
- `个股工作台-v1.0.3-20260910.zip` (341.9 KB, 26 文件)
- 新增测试：`tests/test_v103.py`（16439 B）
- 关键 SHA-256（v1.0.3）：
  - server.py: 6e3ba8dec91e352761389c3ac8491168b04a159f404f35cc46be7b97ec5ba225
  - app.js: 6b67d69913cf3afe8e9fa16d275c39682293e6825e881345cecbeff32d6ba492
  - workbench.db: c4b73bb7932b176211c0f0b199b58efcd134a9b81c1c41dd5263679a557fd993

**仍未解决（沿用 + 增量）**：
- O-003 / O-004 / R-014 / R-015：与 v1.0.2 一致
- R-018：动态执行判断依赖用户手动录入；已用「尚未形成动态执行判断」显式提示
- R-019：SQLite 单文件超 100 万 trades 后索引性能需评估

---

## v1.0.3 审计专用打包（"打包项目送审计"）

用户指令：把项目打包送第三方审计。区别于上轮"全量 ZIP"，本轮做**精挑细选的审计包**：

**打包策略**（与 v1.0.3 全量 ZIP 不同）：
- ✅ 包含：v1.0.3 当前源码 + 当前版本 docx + 设计文档 + 生产 DB + 启动器 + 新生成 AUDIT_MANIFEST.md + build_log.txt
- ❌ 排除：`__pycache__/`、`data/backup/` 5 个历史快照、`archive/v101/` `v102/`、`output/20260910-audit/stage1/` `stage2/intermediate/` `trace/` `working/` 空目录、旧版本 docx (v1.0.0/1/2)、旧 build_docx (v101/102)、`.workbuddy/`、`logs/`

**关键工程难点：清单 SHA 鸡生蛋**
- 三次循环尝试 `manifest ↔ build_log.txt` 互相记录对方 SHA，发现无论哪一边先变化都会让另一边记录的 SHA 过期
- 最终方案：**清单与日志互不记录对方**——两者都只列 15 个送审文件 SHA；ZIP 整体 SHA 由审计方拿到 ZIP 后用 `certutil -hashfile ... SHA256` 直接算（log 中不预存，避免鸡生蛋）
- 这与一般"清单内含自身 SHA"的朴素设计不同，但**实用、稳健、不引入歧义**

**SHA 自洽验证**（最终）：
- manifest 15/15 匹配 ZIP 内文件实际 SHA
- build_log.txt 15/15 匹配
- ZIP 整体 SHA：e241c64f12773b449aaeb83a3cf8cb614b24d00f1254005b5a5ad3a02ee4ac14

**交付物**：
- `D:\个股工作台\个股工作台-审计交付包-v1.0.3-20260910.zip` (126,268 bytes / 123.3 KB / 17 文件)
- `D:\个股工作台\audit_pack_src\AUDIT_MANIFEST.md` (5,753 B) — 15 项送审清单
- `D:\个股工作台\audit_pack_src\build_log.txt` (4,138 B) — 构建日志（含验证方法）
- `D:\个股工作台\output\20260910-audit\build_log.txt` — 同上（ZIP 外副本）

**辅助脚本**（`.workbuddy/tools/`）：
- `build_audit_manifest.py` — 生成 AUDIT_MANIFEST.md
- `build_audit_zip.py` — 打 ZIP 并生成 build_log.txt
- `copy_log_to_pack.py` — 把外部 build_log.txt 同步到 audit_pack_src

**未来类似工作的经验**：
1. 清单/日志互相包含对方 SHA 是死循环；最佳实践是**互不记录**，各自只列已知稳定文件
2. ZIP 整体 SHA 与 log 内容互锁——ZIP 改一次，log 就要改一次，log 改一次，ZIP 又要改一次；接受"log 不预存 ZIP SHA，由审计方外部计算"
3. 审计包的本质：精挑细选 + 范围说明明确；与"全量 ZIP"是两个不同交付物

---

## v1.0.4 精确修复（独立代码审计 12 条 spec）

第三方独立代码审计确认 v1.0.3 主体实现正确但存在 6 个问题（2 个 P0 数据链路 / 2 个 P0 迁移 / 2 个未经批准限制）+ 1 个 fixture 误判。

用户在 long_text_quote 中直接给出 12 条 spec，性质明确：**不增加新功能、不修改投资研究/静态交易计划/状态体系/动态执行层既定定义**。所有 12 条均在用户批准范围内，直接执行。

### 12 条 spec 落地情况

| # | spec | 落地位置 |
|---|------|---------|
| 1 | `/api/securities` 数据链路 enrich() 加 execution_latest | server.py `_execution_latest_row` + enrich() |
| 2 | execution_latest 口径 ORDER BY execution_date DESC, id DESC | 与详情页 get_execution 完全同一 SQL |
| 3 | openExecutionModal 走真实 detail 链路 | app.js `async function openExecutionModal` |
| 4 | test_v103 §F 重写走真实链路 | test_v103.py test_F（不再手工塞 execution_latest） |
| 5 | 已有最新判断时再次读取仍返回原判断 | test_v103.py test_H（8 断言） |
| 6 | 新库 init_db 立即写 schema_version | server.py init_db() is_new 分支 |
| 7 | migrate_v102 不清空用户真实数据 | server.py migrate_v102 删除 UPDATE SET value='' |
| 8 | 新建库 + 升级两组回归测试 | test_v103.py test_I + test_J |
| 9 | 删除 execution_view 后端白名单 | server.py _execution_fields() |
| 10 | 删除 execution_date 未来日期校验 | server.py add_execution_tx() |
| 11 | 删除 fixture 名称猜测 | app.js isFixtureSeedPresent / showSampleNoticeIfFixturePresent / dismissSample / sampleBanner |
| 12 | 保持既有约束 | §A-§E 测试保持 PASS |

### 测试结果

- tests/test_v102.py —— 38/38 PASS（更新 schema_version 期望为 1.0.4）
- tests/test_v103.py —— 60 断言 PASS（§A-§E 23 + §F 重写 10 + §G 7 + §H 8 + §I 5 + §J 3 + §K 4）
- 合计 98 断言全通过

### 生产 DB

- 当前生产 DB **保持 v1.0.3**（未触发升级；首次 init_db 时才会从 1.0.3 升 1.0.4）
- integrity_check ok；SHA c4b73bb79... 未变（测试隔离有效）

### 关键踩坑（v1.0.4 新增）

1. `executing docx` 需要 venv python（`C:\Users\宜春法院\.workbuddy\binaries\python\envs\default\Scripts\python.exe`），不能用系统 python 3.13.12（缺 python-docx）
2. `init_db` 升级时会把 backup 写到 `<db_path>/backup/`，即使 v1.0.4 不再触发（备份代码在 do_migration 内部）——打包脚本必须 monkey-patch 或事后清理 audit_pack_src/data/backup/
3. `build_audit_zip.py` 的 `all_files_for_zip` 必须在写完 build_log.txt 到 audit_pack_src 后**重新扫描**目录，否则 ZIP 内不会含 build_log.txt
4. test_J 手工构造 v1.0.2 DB 时，trades 表必须有 trade_date 等所有 SCHEMA 列（否则 SCHEMA 重复执行时报 "no such column"）
5. test_v103 用 `//` 写注释（中文输入法）会破坏 Python 语法，必须用 ASCII `#`

### 交付物

- `D:\个股工作台\个股工作台-审计交付包-v1.0.4-20260910.zip` (133142 B / 17 文件)
  - ZIP SHA: `25477c4c274cab3c304b513ad92e6039479e1576407529b47bb153d3d318c7b5`
- `D:\个股工作台\output\20260910-audit\stage3\A-H投研交易工作台交付审计文档-v1.0.4.docx` (47939 B)
  - 内含 §15 v1.0.4 精确修复清单
- audit_pack_src/{AUDIT_MANIFEST.md, build_log.txt}
- 辅助脚本：`.workbuddy/tools/{build_audit_manifest,build_audit_zip,copy_v104_sources,copy_log_to_pack}.py`

### 仍未解决（沿用 v1.0.3）

O-003 / O-004 / R-014 / R-015 / R-018 / R-019（同 v1.0.3）。本轮严格遵守"不增加新规则"边界，未引入新开放问题。

## v1.0.4 最终发版打包（2026-09-10 18:40）

- 用户指令：「打包最终版zip」
- 打 ZIP: `D:/个股工作台/个股工作台-v1.0.4-20260910.zip`（131,259 bytes / 128.2 KB）
- ZIP SHA-256: `00d9f4061a387da726df8a05cbfad6b7a437a49a809e7721ad9e2c66b4225355`
- 16 文件：15 送审 + `PACK_MANIFEST.md`
- 关键发现：生产 DB 升级前 schema_version 还是 1.0.3，audit_pack_src 内的副本才是 1.0.4；本次先执行 init_db(db_path=生产 DB) 才升级
- 全量回归通过：test_v102.py 38/38 + test_v103.py 60/60 = 98 断言 PASS
- manifest 15/15 全部 SHA 自洽
- 打包脚本：`.workbuddy/tools/build_v104_final.py`（可复用，下次打 v1.0.5 改 INCLUDE_FILES 即可）
- 验证脚本：`.workbuddy/tools/verify_v104_zip.py`

## v1.0.5 封板准确性修复（2026-09-10 18:55）

- 用户指令：「v1.0.5 封板修复」8 条 spec，由独立代码审计后的裁决结论
- 核心目标：账准（P0 历史补录）+ 前端契约（execution_view 自由文本）+ 版本号统一 + 重写审计 docx

### 8 条落地
1. P0 `add_trade_tx`：candidate 携带 trade_date，按 `(trade_date ASC, id ASC)` 排序（candidate.id=float("inf") 排同日末位）与 compute_position() 同口径；负持仓立即拒绝
2. execution_view 改为 `<input>` + `<datalist>`，4 项仅作建议项
3. execution_date 默认 `today()` 不继承 prev
4. 新增 test_v105 §E/§F/§G/§H 前端契约静态扫描
5. 6 处版本号统一 v1.0.5（TARGET_SCHEMA / server_version / argparse / 启动打印 / 前端头 / docx）
6. 彻底重写 `build_docx_v105.py`，新 docx SHA `3db74c6e...` ≠ v1.0.4 docx SHA
7. 修正 PACK_NOTES/PACK_MANIFEST 文件数量措辞
8. 新增 `tests/test_v105.py`（25 断言）

### 测试结果（123 断言全过）
- test_v102.py：38/38 PASS
- test_v103.py：60/60 PASS（§A-§K 保持）
- test_v105.py：25/25 PASS（§A-§H 新增）
- 生产 DB `schema_version=1.0.5`, `integrity_check=ok`, FK 0 违规

### 最终交付物
- `D:/个股工作台/个股工作台-v1.0.5-20260910.zip` (134,817 B / 131.7 KB)
- ZIP SHA-256: `318ec35196780c2f85522c04903a2c15b5d7ddceac31a35696713c9d57521384`
- 内含 17 文件（清单 16 项 + manifest 自身）
- manifest 16/16 自洽验证
- v1.0.5 docx (45941 B) SHA `3db74c6e...` ≠ v1.0.4 docx SHA（不复用确认）

### 关键踩坑与规律
- `add_trade_tx` 的 _run_in_transaction 装饰器要求 caller 不传 conn；公共函数 `add_trade(sid, body)` 自动管事务
- `chdir`/测试隔离 → `init_db(seed=False, db_path=TMP_DB)` 临时库 + monkey-patch `server.get_db`
- docx 生成器必须用 venv python（`python-docx` 已装的环境）
- "清单 ↔ 日志 互不记录对方" 仍是审计清单 SHA 自洽的最简方案
- test_v105 的 §F 起初把模板字符串当成渲染后 HTML 去匹配 → 错；正确做法是静态扫描 `EXECUTION_VIEWS.map` 数据源数组 + datalist 模板是否引用 map

## v1.0.6 行情一致性修复（2026-09-10 19:35）

- 用户指令：「v1.0.6 最终行情一致性修复」9 条 spec，独立代码审计后裁决 CONDITIONAL PASS
- 核心：多标的"部分获取失败"语义细化（不再"全局成功"骗用户）

### 9 条 spec 落地
1. `get_quotes(symbols)` 区分成功/缺失 symbol，新增 `partial_failure` / `missing_symbols` / `requested_count` / `fetched_count`
3. 缺失证券保留旧 cache + `is_stale=true` + 旧 `market_time`
4. `requested 非空 && fetched 为空` → `error 非空`（即使 fetch_tencent() 不抛异常）
5. 部分成功 → `error='部分行情未更新（N/M）：symA、symB...'`
6. 卡片显示 `staleBanner='上次成功行情 · 本次刷新未成功'`
7. `attention()` 检查 `isStale`：stale 时跳过价格派生事实（z1/z2/z3/danger），保留 status / wall / core_validation
8. 新增 `tests/test_v106.py`：§A 11 + §B 8 + §C 3 + §D 8 = 30 断言
9. 保持 v1.0.5 123 断言继续通过

### 测试结果（153 断言全过）
- test_v102.py：38/38 PASS
- test_v103.py：60/60 PASS
- test_v105.py：25/25 PASS
- test_v106.py：30/30 PASS（§A 部分成功 + §B 全部失败 + §C stale 不进 attention + §D 静态契约）
- 生产 DB：schema_version=1.0.6, integrity_check=ok, FK 0 违规

### 6 处版本号全统一 v1.0.6
- TARGET_SCHEMA_VERSION = '1.0.6'
- Handler.server_version = 'Workbench/1.0.6'
- argparse description = 'v1.0.6'
- 启动打印 = 'v1.0.6 已启动'
- 前端头 = 'v1.0.6'
- docx = v1.0.6

### 关键踩坑
- `setup_module_with_tmp` 必须 monkey-patch `server.get_db` 返回 `sqlite3.connect(path, row_factory=sqlite3.Row)`，否则 `dict(row)` 会 TypeError
- 前端 `priceFacts` 不在 server.py，必须在测试里 Python 等价复写
- `re.match(r'^\| (\d+) \|...')` 在 shell heredoc 里反斜杠被吃，写成 verify 脚本更稳
- docx 必须"彻底重写"而非 cp+replace（v1.0.4 那次被否决就是因为复用）
- v1.0.6 docx SHA `d69bfd33...` ≠ v1.0.5 docx SHA `96c1b1f0...`（不复用确认）

### 最终交付物
- `D:/个股工作台/个股工作台-v1.0.6-20260910.zip` (137,352 B / 134.1 KB)
- ZIP SHA-256: `2e826457cf914ba3b9c57c149b810d7ec8e86c6bcbab67bc38d2d53f239e4fdf`
- 内含 18 文件（清单 17 项 + manifest 自身）
- manifest 17/17 全部 SHA 自洽 ✓
