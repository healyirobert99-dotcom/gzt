# ============================================================
#  A/H 投研交易工作台 —— Git 忽略规则
#
#  说明：本项目按「完整留档」策略托管，源码、测试、样例、
#  审计材料、数据快照与历史归档均**有意**纳入版本管理。
#  此处仅排除纯编译产物与操作系统/编辑器产生的垃圾文件。
# ============================================================

# ---- Python 编译产物（可由源码随时重建）----
__pycache__/
*.py[cod]
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
*.egg-info/

# ---- 虚拟环境（如启用，不入库）----
.venv/
venv/
env/

# ---- 根目录 0 字节残留文件 ----
# 真实数据库位于 data/workbench.db，此处只是历史误建的占位文件。
/workbench.db

# ---- SQLite 运行时伴生文件（只读连接也会生成 -shm/-wal，绝不入库）----
*.db-shm
*.db-wal
*.db-journal

# ---- 验证脚本的临时产物 ----
# ab_out/：agent-browser 每次运行把逐条命令的原始输出落成 ab_NNN.txt（数量多、信息重复），
#          整理后的结论保存在同目录的 *_out.txt 里，故原始文件不入库。
# sandbox_arch/：沙箱服务用的「真实库副本 + app/ 副本」，纯临时，绝不入库
#          （避免把同一份数据库再存一遍）。
.tmp_v108x/verify/ab_out/
.tmp_v108x/verify/sandbox_arch/

# ---- 操作系统垃圾 ----
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# ---- 编辑器 / IDE ----
.vscode/
.idea/
*.swp
*.swo
*~
