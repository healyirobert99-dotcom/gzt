归档边界检查: 补「转发器」同型面(§E8/§E9 + §D#10d/e) 并闭口 S.secs 消费点

"同一个 id 从另一个入口进来"这一支线闭合。抽屉上「··· 更多」是**纯 id 转发器**:
openMoreActions(id) 自己不查标的, 只把 id 原样塞进 4 个 onclick, 由各弹窗内部走 findSec。
若有人图省事在转发器里先查一次活跃列表, 4 项会一起静默失效, 而各下游自身全绿,
静态读代码看不出。

离线契约 tests/test_security_archive.py 122 -> 124:
- §D#10d openMoreActions 函数体内不出现 S.secs/findSec, 且 openModal( 恰好 1 次
- §D#10e 4 个 onclick 都是纯 id 透传 (4/4)

浏览器边界脚本 browser_e2e_archive_edge.py 32 -> 37:
- §E7 保持 4 条(详情页 4 入口, 含对照组「··· 更多」)
- §E8 新增 4 条: 「··· 更多」菜单打开后再点里面 4 项; 判据取各弹窗独有字段标签
  (新状态 / 修改说明(记入台账) / A/H 两地上市关联 / 记录内容(必填)), 防"随便弹个窗"假绿
- §E9 新增 1 条: 对照组, 同一条点击链在活跃标的上同样成立

负向验证:
- neg_archive_import_contract.py 扩到 6 次注入(M5 转发器里加活跃列表查找; M6 某一项改为对象)
  -> 12/12, 6 次注入目标断言全部翻红
- neg_findsec_browser.py 新增(浏览器层, 6 断言): 回退 findSec 为单级查找后,
  目标 7 条(§E7#1~3 + §E8#1~4)全部翻红, 而对照组 §E7#4(不走 findSec) 与
  §E9#1(活跃标的)保持绿 => 排除"整体坏了"的混淆解释

全项目 S.secs 消费点闭口: 17 个消费点逐条判过, 只有 2 处构成缺陷(已修)。
易误判处已排除: uiLifecycleSort 的 indexOf 入参恒来自 S.secs 自身(永不 -1);
前端无全局台账/交易页, 台账与流水都在详情抽屉走真实详情接口, 不受归档影响。

数字: 离线全套 682 -> 684 PASS / 0 FAIL, 11 套件逐套与基线一致;
浏览器 E2E 38/38, 边界 37/37, A/H 11/11; 进程内 check_archive_import.py 38/38;
真实库 SHA-256 全程 4f4832aa51d00b51...(与会话开始时相同)。
未升版本号(用户拍板只改代码+测试), 故不出交付包。
