归档并发探针: 归档/恢复 x append-only 台账的不变式(§C#15~#15d) + 8 次注入

补齐项目自己写下的那条铁律("凡 append-only 写入的改动必须跑并发探针")。
归档/恢复每次追加 1 条 decision_ledger, 而 _set_archived_at 是
「读校验 -> 写状态 -> 追加台账」三段式, 与 v1.0.9 漏出的 R-027 同形。
顺序路径全绿不能证明无缺陷。

结论: 并发安全, 无缺陷 —— 但现在有证据了。

新增 .tmp_v108x/verify/concurrency_archive_probe.py (12 断言):
- §1 纯归档 12 轮 x 8 线程打同一标的: changed=True 严格 == 1/轮,
  台账增量 == changed 数, 无幽灵行, 无其它异常, 终态必为已归档
- §2 混合并发 12 轮 x 8 线程(归档/恢复交替): 两类台账增量各等于对应方向成功数;
  终态与成功次数奇偶守恒(每次 changed=True 都是一次真翻转)
- 192 次调用里真的撞上 SQLITE_BUSY 32 次(约 17%), 全部按受控冲突处理
- §3 真实库逐字节未变

新增离线契约 tests/test_security_archive.py §C#15~§C#15d (124 -> 128):
- 每轮 changed=True 恰好 1 个(幂等, 不重复追加台账事件)
- 台账增量恰等于 changed=True 数(append-only 不被污染)
- 并发冲突一律 409, 绝不出现 500
- 并发归档后终态为已归档

容易退回的细节: _is_db_busy_error 用 code & 0xFF 取主码, 故
SQLITE_BUSY_SNAPSHOT(517) 也被正确归入 409 族。若"简化"成只比对 5/6,
WAL 下的快照升级失败会变成 500。探针里那 32 次 BUSY 正是这条路径的真实来源。

负向验证 neg_archive_import_contract.py 扩到 8 次注入 (12 -> 16 断言):
- M7 去掉 _set_archived_at 的幂等守卫 -> 单轮 6 个 changed=True 且出现 409 => §C#15 红
- M8 写了台账却返回 changed=False(幽灵行) => §C#15b 红
- 8 次注入全部命中目标断言

探针自身修了一个 bug (值得记): 首跑 4 条 FAIL 差点当成产品缺陷, 实为 delta 算式
把 other 写成了总增量, 每轮多出"幽灵 +1"。改成先算三个 delta 再相减即全绿。
教训: 探针报红的第一步永远是怀疑探针自己。

数字: 离线全套 684 -> 688 PASS / 0 FAIL, 11 套件逐套与基线一致;
浏览器 E2E 38/38, 边界 37/37, A/H 11/11;
进程内 38/38, 并发 12/12; 真实库 SHA-256 全程 4f4832aa51d00b51...
未升版本号(用户拍板只改代码+测试), 故不出交付包。
