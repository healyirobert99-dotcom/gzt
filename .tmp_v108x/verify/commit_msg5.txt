fix(verify): 修验证脚本自身的两处探针缺陷（隔离性判据 / box 读数）

本次任务实为「确认 09-15 已实现的卡片「×」归档按钮」，未改任何产品代码。
但真实环境跑 E2E 时暴露出验证脚本自身两个缺陷，必须修：

1. §6 隔离性错误地用 sha256 判定「真实库未变」
   - 8765 上用户自己的实例每 60 秒刷新行情，必然改真实库字节
   - 上一版把整行（含 current_price 两列）算进业务指纹 => §6#1 必红
   - 改为：业务指纹排除行情两列 + 「字节变化只限于行情两列」+ 记录写者 PID + 打印变化明细
   - 这是探针假红，不是产品缺陷

2. `get box` 读数带标签，re.findall(r'\d+') 会把 URL 里的 8805 吃进来
   - 导致 width=8805.0，按它裁剪必然裁到空白
   - 改为逐键正则取值，并在裁剪后做像素差自检

配套：
- 新增 neg_e2e_isolation.py（负向验证两条新断言各自守不同的东西，非装饰品）
- 新增 cleanup_sandbox_archive.py（收尾还原走 unarchive 接口，浏览器点击会被 60 秒重渲染替换 DOM）
- browser_e2e_archive_keyboard.py 同步同一处隔离性修法
- 取证脚本 shot_card_delete.py / shot_archived_section.py / crop_shots.py
- 取证产物 output/20260916-卡片删除按钮确认/
- ENV-TRAPS ⑯⑰⑱、MEMORY.md 验证约定第 7 条、当日日志

验证结果：E2E 38/38、键盘 24/24、离线 693/693；负向 A/B 变体按设计翻红。

注：data/workbench.db 未纳入本次提交——其改动来自用户自己实例的行情刷新
与用户亲手归档紫金矿业，属真实业务操作，如何入库待用户决定。
