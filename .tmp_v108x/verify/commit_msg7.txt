verify(archive): 补完「连点两下」影响半径 + 排掉「看不到」的真凶（未改产品代码）

用户重复提出同一需求（卡片删除叉）。该功能 09-15 已实现（1829eed），本轮不重写，
只做两件事：排掉"为什么看不到"，并补完上一轮被网络中断打断的双写半径探针。

A. 为什么"看不到"——三个可能逐一排除，揪出一个真缺口
  1. 实例服务的就是当前代码：8765 返回的 app.js 与磁盘逐字节相同
     （sha256 5e77c08c4a9828ee / 117335 字节），进程当天 09:30 启动，晚于源码改动。
  2. 三条 CSS 显形规则（基础 / :hover / :focus-visible）全在，与磁盘一致。
  3. 桌面只有委托启动器，Downloads / Documents 无副本 → 只有这一份。
  ★ 缺口：交付包 个股工作台-v1.0.10-20260911.zip（09-11 打）里 card-archive 出现 0 次
    —— 包早于 09-15 的功能。若跑的是解压包就拿不到「×」，需重新打包。

B. 连点两下提交的真实影响半径（probe_double_submit_radius.py）
  5 个弹窗里 4 个会双写：
    trades             +2   ← 业务表
    execution_reviews  +2
    research           +2
    decision_ledger    +2
    trade_plans         0   （是 UPDATE，安全）
  根因：openModal 无在途守卫 + 服务端这 5 条全无去重/唯一约束。
  create_security 有查重、archive 有幂等守卫 → 二者免疫。
  属其它功能，未擅自改，已记入开放项。

C. 取证
  新增 annotate_hover_hint.py → 09-哪里悬停-中文标注.png（红圈 + 箭头 + 中文说明，
  含像素自检）。影响半径日志归档为 10-连点两下影响半径-5个弹窗.txt。

D. 记忆
  MEMORY.md 从 5133 字符压到 ~3900（细则继续下沉到 ENV-TRAPS / DEFECTS / 技能），
  并补入"交付包不含新功能"这条开放项。DEFECTS.md 换掉原先只有一句的
  openNoteModal 记录，改为完整的影响半径表 + 交付包缺口条目。

全程未改任何产品代码，版本号不动，仍为 v1.0.10。data/workbench.db 未提交（用户自身
实例的行情刷新 + 用户亲手归档紫金矿业），待用户拍板。
